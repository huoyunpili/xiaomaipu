# 闲鱼小卖家订单协同、担保款与订单利润管理 PRD

> 文档版本：V1.31
> 更新日期：2026-09-15
> 产品阶段：MVP 重新定位
> 目标用户：通过微信联系供应商代发、每天处理多笔闲鱼订单的小卖家
> 分发方式：GitHub 下载，本地单店运行
> 文档状态：业务流程已经按实际经营重新梳理；现有代码尚未完全对齐本版

## 1. 产品定位

本产品是一套运行在卖家自己电脑上的轻量订单后台。

它不建设大型电商平台，也不要求卖家维护完整采购、库存或会计账。它只解决每天最容易出错、最容易造成损失的五件事：

1. 找出已经付款、现在需要发货的订单；
2. 把型号规格和收件信息汇总成一张图发给供应商；
3. 找出需要退款或需要向供应商追回货款的订单；
4. 计算仍在平台担保流程中的订单金额，并显示订单产生时间和预计回款时间。
5. 计算每笔订单的预计利润或实际利润，并分别汇总每日预计利润与所选时间段的实际利润。

软件通过 GitHub 提供下载和更新。每位使用者在自己的电脑上安装，创建自己的管理员账号，配置自己的闲管家 API，经营数据和 API 密钥保存在自己的本地实例中。基础使用不依赖项目方云服务器、公共账号或订阅服务。

## 2. 真实经营流程

### 2.1 客户已经付款

客户付款后订单正式进入日常履约流程。

系统自动读取：

- 平台订单号；
- 下单时间和付款时间；
- 型号、颜色和规格；
- 数量；
- 实付金额；
- 收件人、电话和完整地址；
- 平台订单状态、退款状态和物流状态。

符合以下条件的订单进入“已付款待发货”：

- 客户已经付款；
- 订单没有关闭；
- 没有进入需要剔除的退款状态；
- 闲管家显示订单尚未发货。

### 2.2 向供应商发送发货信息

卖家通常通过微信把发货信息发给供应商。当前痛点是地址与商品规格分散在不同页面或消息中，聊天内容穿插后容易漏发、错发和贴错快递单。

系统按供应商汇总待发货订单，生成一张可以直接保存或复制到微信的长图。

长图必须包含：

| 字段 | 用途 |
|---|---|
| 型号、颜色、规格 | 解决当前最主要的信息缺失 |
| 数量 | 防止少发或多发 |
| 收件人 | 对应客户 |
| 手机号 | 供应商下单发货 |
| 省市区和详细地址 | 供应商下单发货 |
| 订单备注 | 特殊要求 |

长图按供应商分别生成，标题示例：

“2026年9月15日发货单｜供应商A｜共8单10件”

系统不要求逐单登记“是否已经向供应商付款”。该数据没有可靠 API 来源，手工维护成本高，也不是生成发货单和判断平台发货状态所必需。

首次使用某个商品或规格时，可以建立“商品/规格 → 默认供应商”的映射。以后同款订单自动归入对应供应商；临时更换供应商时允许单次修改。

每次导出保存批次时间和包含的订单号，目的是后续查错和重新导出，不代表供应商已收款或已经发货。

### 2.3 客户付款后申请退款

退款会打乱当天发货计划。系统必须在每次生成发货单时重新读取最新订单和退款状态。

符合下列任一条件的订单不得进入新的发货单：

- 订单已经关闭；
- 已经全额退款；
- 正在退款且业务规则要求暂停发货；
- 平台明确取消发货责任。

如果订单之前已经进入某个发货批次，随后发生退款，系统显示醒目的“停止发货/联系供应商”提醒，并保留原批次记录，不能静默删除历史。

### 2.4 供应商发货

闲管家订单接口包含订单状态、发货时间、发货方式、快递公司和快递单号。系统通过 API 自动区分未发货和已发货。

状态变化规则：

- 每次自动同步后，实时显示 API 当前标记为“未发货”的全部已付款订单；
- 平台显示已发货：从待发列表移出，进入“已发货待完成”；
- 系统只采用平台的整单发货状态，不拆分发货进度；只要 API 尚未显示整单已发货，就继续显示为“未发货”；
- 快递单号发生变化：保留最新信息和更新时间；
- 平台字段冲突或缺失：显示“待核对”，不能擅自视为已发完。

卖家不需要在本系统重复填写已经能够由闲管家取得的快递单号。

### 2.5 客户满意并完成交易

客户收到商品后，订单仍可能处于平台担保阶段；只有平台明确显示交易已经完成，货款才视为已经转入商家账户。已完成订单必须立即移出担保金额计算。

系统展示每笔订单：

- 订单产生时间；
- 客户付款时间；
- 发货时间；
- 平台确认/交易完成时间；
- 预计回款时间；
- 当前担保金额；
- 已等待时间；
- 是否超过预计回款时间。

订单完成后，移出“待完成”和“担保中”列表。

### 2.6 客户退货退款

客户不满意并申请退款时，系统将订单移入“退款售后”。

平台已经完成退款时，即使原订单没有进入“交易成功”，这部分钱也已经不再属于卖家的担保款。系统以经过真实订单验证的退款完成状态为判断依据，并使用 refund_amount 和 refund_time 记录实际退款金额与时间；同步确认后立即从担保中金额、预计销售额和预计利润中扣除。

如果该订单对应供应商代发，系统还要生成“待向供应商追回”任务，防止卖家给客户退款后忘记追回供应商货款。客户侧退款已经完成与供应商是否把货款退给卖家是两件事，不能因为供应商尚未退款，就继续把客户已退金额算作担保款或预计收益。

退款任务至少显示：

- 商品型号、规格和数量；
- 客户全额退款金额；
- 平台退款状态；
- 退货快递单号；
- 退货运费损失（选填）；
- 对应供应商；
- 供应商待退成本金额（系统自动计算）；
- 是否已经确认收回；
- 等待时间。

全额退款不等于产生运费损失。未发货订单退款时，退货运费损失保持默认值 0；已经发货的订单也只有在卖家确实承担了退货运费时才由用户填写实际金额。

系统不要求记录每个正常订单是否向供应商付款。只有发生售后时才需要确认供应商退款结果。

闲管家 API 不提供订单的供应成本。用户需要在对应商品类型上手工填写一次单件成本，系统以后自动匹配所有同类型订单。供应商待退成本金额就是本次退货对应商品的成本合计，由系统使用用户填写的成本自动计算，不要求用户逐单填写。系统保存的“订单成本快照”只是订单匹配成本表后的本地记录，不是从 API 读取的字段。

订单出现时该商品类型已有成本的，系统直接保存成本快照；当时缺少成本的，用户补充该类型成本后，系统自动为所有匹配的缺成本订单补算。以后修改商品类型成本只影响新订单，不能覆盖已经保存的旧订单成本快照。成本仍缺失时显示“待补成本”，订单继续保留在待退列表。

## 3. MVP 核心工作区

### 3.1 待发货与发货单导出

回答：“现在有哪些订单需要供应商发货？”

默认按付款时间从早到晚排列。每张订单卡优先展示型号规格、数量、收件地区和等待时长。支持按供应商筛选和选择订单。

待发货页面直接提供“导出发货单”按钮，不设置独立的发货单页面。主要操作：

- 查看订单；
- 修改本单供应商；
- 按供应商筛选；
- 选择需要导出的订单；
- 点击“导出发货单”，按供应商自动分组并生成适合微信发送的 PNG 长图；
- 默认只导出已付款、未退款、未发货的有效订单；
- 导出前重新同步并检查订单状态；
- 保存导出批次，退款或状态变化后提示重新生成；
- 在待发货页面查看历史导出批次并回查订单。

### 3.2 已发货待完成

回答：“哪些订单的货已经发出，但钱仍处于担保或待回款阶段？”

每笔订单显示金额和时间轴，支持按预计回款日期排序。超过预计日期仍未进入完成状态时突出提醒。

### 3.3 退款售后

回答：“哪些退款订单需要暂停发货、跟进退货或向供应商追回货款？”

分为三个清晰阶段：

1. 暂停发货；
2. 客户退款/退货处理中；
3. 待向供应商追回或等待供应商退款。

支持按供应商生成退款清单长图，供微信发送和催办。

### 3.4 订单利润

回答：“每笔订单赚了多少钱，今天以及选定时间段一共赚了多少钱？”

成本不能通过闲管家 API 获得。用户在对应商品类型上手工填写一次单件成本，系统自动复用于同类型订单，并在订单付款时保存本地成本快照、计算逐单利润。未完成订单显示预计利润，成功成交订单显示实际利润，全额退款订单显示售后实际损失。系统支持查看：

- 每笔订单的销售额、商品成本、平台费、退货运费损失和利润；
- 按当天付款订单计算的每日预计利润；
- 按成交完成日期统计的时间段实际利润；
- 每日预计利润和成交实际利润趋势；
- 缺少成本、退款中、发生亏损和填写了退货运费损失的订单明细。

修改商品当前成本只影响之后的新订单，不重算过去订单；如历史订单成本确实录错，必须通过订单成本更正并保留原因。

## 4. 担保金额和回款时间

### 4.1 订单产生时间

订单产生时间直接使用闲管家返回的 order_time。页面显示北京时间，并保留原始来源和最近同步时间。

### 4.2 担保中订单

只有同时满足以下条件的金额才计入“担保中”：

- 客户已经付款；
- 订单尚未完成，货款还没有按平台状态转入商家账户；
- 订单没有关闭；
- 订单没有完成全额退款。

计算时只区分三种情况：

- 已完成订单：客户实付金额已经转入商家账户，担保金额为 0；
- 已付款、未完成且没有全额退款的订单：单笔担保金额就是客户实付金额；
- 已全额退款订单：钱已退回客户，担保金额为 0。

本产品不处理部分退款。refund_amount 和 refund_time 只用于确认全额退款金额和退款时间。只有退款申请、资金尚未实际退回客户时，单列为“退款风险订单”；平台一旦确认全额退款完成，就把整笔订单从担保金额中移出。

担保金额合计：

所有“已付款、未完成且未全额退款”订单的客户实付金额之和。

1.6% 平台费只用于预计利润和成交利润计算，不从担保金额中扣除。页面统一显示“按订单推算的担保中金额”。

### 4.3 预计回款时间

预计回款时间只用于安排经营，不要求预测实际到账日。系统统一按照平台公开的通用交易超时规则计算参考日期：

- 本产品范围内的正常快递订单，买家没有确认收货且没有申请退款时，平台从进入“卖家已发货”状态起 10 天自动确认交易；这里的 10 天按 10 × 24 小时理解，不从物流签收时间起算；
- 买家可在剩余 72 小时时申请一次延长收货，通常延长 3 天；
- 退款申请或客服介入会暂停原交易倒计时，特殊订单以闲鱼订单页显示为准；当前闲管家订单 API 不能直接读取这个正常交易倒计时。

系统按以下优先级显示：

1. 订单已经完成时，使用 confirm_time 显示“实际确认时间”，不再显示预计日期；
2. 正常订单统一显示“平台参考回款日”：发货后第 10 天；
3. API 能识别订单正在退款或客服介入时，显示“回款时间暂停参考”；
4. 没有发货时间时，显示“预计回款时间待确认”；
5. 售后接口中的 timeout_time、timeout_status、timeout_type 只有在真实订单验证含义后，才可以显示为“售后处理截止时间”，不能当作正常订单的预计回款时间。

当前闲管家订单接口公开字段包含 consign_time 和 confirm_time，但没有每笔订单未来的自动确认倒计时。售后详情接口另有 timeout_time、timeout_status 和 timeout_type，但公开文档没有解释这些字段的完整含义和枚举。因此正常订单只能按通用规则计算参考日；售后截止时间必须经过真实退款订单验证后再启用。

本规则于 2026-09-15 根据闲管家收货帮助页及其转载的《闲鱼交易超时说明》核验：本产品使用的正常快递订单从卖家发货起 10 天，规则中的“天”按 24 小时计算。买家提前确认、延长收货、退款、客服介入或特殊订单会改变实际时间，因此页面必须写“平台参考回款日”，不能写成保证到账日期。

首页同时显示：

- 今日预计回款金额；
- 未来三天预计回款金额；
- 已超过预计时间的订单数和金额；
- 当前担保中订单数和金额。

预计日期规则在设置中统一维护，不要求用户逐单填写。

## 5. 订单利润计算与汇总

### 5.1 成本配置

闲管家 API 不提供采购或供应成本，成本必须由用户维护。用户只需在每种商品类型首次出现时填写一次单件成本，后续同类型订单自动使用，不要求逐单录入。系统根据 API 返回的商品标识和型号规格匹配商品类型；同一类型确有不同规格成本时，用户可以分别设置。

成本变化时建立新版本并记录生效时间。订单进入已付款状态时，系统把当时匹配到的用户成本保存为本地快照：

- 商品和规格；
- 数量；
- 客户实付金额；
- 单件供应成本；
- 成本来源为用户维护的商品类型成本，并记录对应版本。

这里的成本快照是系统复制的用户配置值，不是 API 返回的订单成本。当前成本变化不能覆盖旧订单成本快照；缺成本订单在用户首次补充该商品类型成本后自动生成快照并补算。

### 5.2 计算口径

成功成交订单的平台费：

客户实付金额 × 1.6%

已付款且预计正常成交订单的预计利润：

客户实付金额 −（有效数量 × 单件供应成本快照）− 预计平台费

成功成交订单的实际利润：

客户实付金额 −（成交数量 × 单件供应成本快照）− 平台费

平台明确交易成功后，按客户实付金额的 1.6% 计入平台费，并把这笔订单从“预计利润”转为“实际利润”。尚未完成但没有退款的订单，在预计利润中先按 1.6% 估算平台费；如果订单后来全额退款，则该订单销售额和平台费都归零。

全额退款订单不产生销售利润，售后实际损失只计算用户填写的退货运费：

售后实际损失 = 退货运费损失

“退货运费损失”为售后订单的选填金额，默认是 0。未发货退款或没有产生运费损失时无需操作；已经发货且因质量问题等原因由卖家实际承担退货运费时，用户填写实际金额，系统记录为负向损失并保留修改记录。

页面必须标明“成功成交订单的平台费按客户实付金额的 1.6% 计算；全额退款订单不收平台费”。未完成订单的结果标为“预计利润”，成功成交订单的结果标为“实际利润”，退款处理完成后的运费损失标为“售后实际损失”。实际利润是本系统根据销售额、用户成本和平台费计算的经营结果，不等同于包含工资、房租、税费等全部科目的会计报表利润。

计算规则：

- 只有已经付款的订单参与统计；
- 已关闭且没有有效成交的订单不参与统计；
- 成功成交订单按客户实付金额的 1.6% 计算平台费；
- 已付款、尚未完成且没有退款的订单，在预计利润中先按客户实付金额的 1.6% 估算平台费；
- 全额退款完成后，整笔订单从预计销售额中剔除，平台费归零；退货运费损失默认是 0，只有用户确认实际发生并填写后才记录为负向售后损失；
- 本产品不处理部分退款；
- 只有退款申请、尚未实际退款的订单单独列为风险，不与正常订单混在一起；
- 缺少成本的订单不把成本当作 0，也不计入预计利润合计；页面单列缺成本订单数和涉及销售额；
- 预计利润为负数时按实际负数显示，不截成 0。

### 5.3 每日预计利润与时间段实际利润

每日预计利润按订单付款时间 pay_time 归属到北京时间自然日。当天已付款、尚未全额退款的订单参与预计；未完成订单使用预计利润，已经完成的订单使用实际利润。每日预计利润由这些订单的逐单利润相加得到，后续退款时按最新状态回算付款日数据。

时间段实际利润只统计已经成交完成的订单。系统使用闲管家 API 返回的 confirm_time 作为成交完成时间；用户选择的开始日和结束日均包含当天，只有 confirm_time 位于该区间的已完成订单参与统计。未完成订单、退款申请中订单和全额退款订单不进入时间段实际利润。

全额退款订单只有在用户填写了实际发生的退货运费时，才记录“售后实际损失”；未发货退款或没有运费损失时为 0。售后实际损失单独统计，不混入“已成交订单实际利润”。缺少成本的已完成订单不进入实际利润合计，并提示用户补充该商品类型成本。

统计页分成两个明确区块：

每日预计利润：

- 当日付款订单销售额；
- 商品成本；
- 预计平台费；
- 预计利润；
- 缺少成本订单数；
- 可追溯的订单明细。

时间段实际利润：

- 成交完成订单销售额；
- 商品成本；
- 平台费；
- 实际利润；
- 成交完成订单数；
- 缺少成本订单数；
- 按成交完成日期汇总的每日明细或趋势图；
- 单独显示该时间段记录的售后实际损失。

### 5.4 利润统计页面展示

利润统计页必须让用户先看到结果，再能直接核对每一单的计算过程。页面顶部提供两个切换项：

- “每日预计利润”：默认显示今天，可切换日期；
- “时间段实际利润”：选择开始日期和结束日期，只统计该区间成交完成的订单。

时间段筛选是利润统计页的固定功能，放在页面标题和汇总卡之间，用户无需进入更多设置即可使用：

- 提供开始日期、结束日期和“查询”按钮，开始日与结束日均计入统计；
- 提供“今天”“近 7 天”“本月”“上月”和“自定义”快捷选项，默认选择“本月”；
- 时间段实际利润按北京时间和成交完成时间 confirm_time 筛选；每日预计利润切换日期时按付款时间 pay_time 筛选；
- 用户改变时间段后，汇总卡、趋势图、逐单计算表、合计行和导出内容必须同时更新，不能出现不同区域时间范围不一致；
- 页面明确显示当前统计范围，例如“2026-09-01 至 2026-09-15”，刷新页面后保留本次选择；
- 开始日期晚于结束日期时不执行查询，并在日期控件旁提示用户修正；
- 所选时间段没有符合条件的订单时显示空状态和当前筛选范围，汇总金额显示 ¥0.00。

顶部汇总卡依次显示：

1. 订单数；
2. 销售额；
3. 商品成本；
4. 平台费；
5. 利润；
6. 缺少成本订单数。

汇总卡下方直接显示逐单计算表，不要求用户进入订单详情才能看懂利润：

| 字段 | 显示内容 |
|---|---|
| 日期 | 每日预计利润显示付款时间；时间段实际利润显示成交完成时间 |
| 商品 | 商品类型、型号规格和数量 |
| 订单状态 | 未完成显示“预计”，成交完成显示“实际” |
| 客户实付 | API 返回的订单实付金额 |
| 商品成本 | 数量 × 用户填写的单件成本 |
| 平台费 | 客户实付金额 × 1.6% |
| 运费损失 | 正常订单为 0；实际发生时显示用户填写金额 |
| 计算过程 | 例如“¥200.00 − ¥120.00 − ¥3.20 = ¥76.80” |
| 单笔利润 | 本单最终计算结果 |

表格底部必须提供合计行，分别合计销售额、商品成本、平台费、运费损失和利润。顶部利润汇总、表格合计以及逐单利润之和必须完全一致。

展示规则：

- 默认先显示金额和计算式，状态解释放在鼠标提示或展开详情中；
- 支持按日期、利润高低和商品类型排序；
- 缺少成本的订单整行突出显示“待补成本”，不显示虚假利润，也不进入利润合计；
- 点击任一金额可以看到其来源字段和计算公式；
- 金额统一保留两位小数；
- 每个汇总数字都能下钻到组成该数字的订单列表。

## 6. 订单状态分类

系统面向用户显示业务语言，不直接把平台数字代码作为主界面文案。

| 业务状态 | 含义 | 主要去向 |
|---|---|---|
| 已付款待发货 | 已付款、有效、未发货 | 发货清单 |
| 已发货待收货 | 已取得发货状态/单号，交易未完成 | 担保中 |
| 已收货待回款 | 物流已签收或等待平台确认，交易尚未完成 | 担保中 |
| 已完成 | 平台明确交易完成，货款视为已进入商家账户 | 全部订单中的“已完成”，不计入担保中 |
| 退款处理中 | 已申请退款或退货退款 | 退款售后 |
| 已退款 | 平台退款完成 | 退款售后及全部订单中的“已退款” |
| 已关闭 | 订单取消或关闭 | 全部订单中的“已关闭” |
| 待核对 | API 字段不完整、冲突或语义未验证 | 人工核对 |

订单主状态、退款状态和本地处理状态分别保存，避免退款状态覆盖原来的发货历史。

## 7. 数据来源与自动化边界

### 7.1 闲管家 API 自动取得

根据当前官方订单和售后接口，可接入：

- 订单号；
- 订单状态；
- 下单、付款、发货、确认、取消和更新时间；
- 商品标识、型号规格、数量和单价；
- 客户实付金额和退款金额；
- 收件人、手机号、省市区和详细地址；
- 快递公司、快递单号和发货方式；
- 售后编号、售后类型、退款状态、申请/退款时间；
- 退货物流和平台处理截止时间。

当前实现必须继续验证状态枚举、隐私订单字段和空值。文档示例存在字段不等于每个账号和每笔订单都能取得完整值。

### 7.2 一次配置、持续复用

用户只需要在首次遇到某种商品类型时维护：

- 默认供应商；
- 供应商微信备注名称；
- 该商品类型的单件供应成本；
- 必要的发货备注。

这些数据由用户填写并保存在本地，不从闲管家 API 读取。后续同类型订单自动复用，不能要求每天或逐单重新填写。

### 7.3 低频人工确认

只有异常情况需要人工操作：

- API 无法确定订单状态；
- 商品没有供应商映射；
- 一单临时更换供应商；

- 售后供应成本未知；
- 确认供应商退款已经收回。

### 7.4 明确不收集

MVP 不要求用户逐单输入：

- 是否已经向供应商付款；
- 供应商付款时间；
- 自己的银行卡、微信或支付宝流水；
- 每笔平台实际到账；
- 完整库存盘点。

用户不需要手工输入每笔订单利润。系统必须根据订单实付金额、用户维护的成本、1.6% 平台费、退款状态和选填的退货运费损失，自动计算每笔订单利润。

## 8. 信息架构

主导航只保留高频入口：

1. 工作台；
2. 待发货；
3. 待完成；
4. 退款售后；
5. 利润统计；
6. 全部订单；
7. 设置。

API 同步状态、商品与供应商映射、历史导入和账号管理放在设置中，不占据日常主导航。

### 8.1 工作台首屏

首屏显示四个任务区块和一个经营指标：

| 区块 | 核心数字 |
|---|---|
| 已付款待发货 | 订单数、件数、供应商数、最早等待时间、最近发货单导出时间 |
| 担保中 | 订单数、按订单推算金额、今日预计回款 |
| 退款售后 | 处理中订单数、待追回金额、最久等待时间 |
| 今日预计利润 | 预计销售额、已知成本、预计平台费、预计利润、缺成本订单数 |

同步异常放在首屏下方。页面优先使用型号规格、金额、数量和时间，解释文字通过帮助和详情展开，避免首页堆叠大量定义。

### 8.2 全站人因与视觉设计原则

网站设计必须遵循人体工程学和人因设计原则，让用户用较少的阅读和记忆完成操作。所有页面统一遵循以下要求：

#### 信息层级与整体布局

- 每个页面只突出一个主要任务，主操作按钮放在页面标题附近或列表操作区的固定位置；
- 先显示需要处理的任务和关键数字，再显示订单明细，最后提供解释和设置；
- 相关信息放在同一视觉分组中，使用留白、分区标题和卡片建立层级；
- 控制单屏文字密度，长说明默认折叠到“查看说明”或帮助入口；
- 表格列只保留当前任务需要的信息，次要字段在展开详情中显示；
- 桌面端充分利用横向空间，但内容不能无限拉宽；手机端自动改为单列卡片，不能依赖横向滚动才能完成核心操作；
- 主要按钮、筛选器和订单卡在不同页面保持相同位置与样式，减少重新学习。

#### 图片优先

- API 能取得商品图片时，待发货、待完成、退款售后和订单详情优先显示商品缩略图，帮助用户快速识别货品；
- 缩略图缺失时显示统一占位图，不用大段文字解释图片失败；
- 点击缩略图可以查看大图，但图片不遮挡型号规格、金额和状态；
- 操作流程、首次配置、空状态和常见错误能用示意图或步骤图说明时，优先使用图片配合短句，避免连续大段文字；
- 图片用于帮助识别和理解；收件地址、金额、型号规格等需要复制或核对的信息仍保留文字；
- 发货长图继续按已确认字段生成，不因为网页显示商品缩略图而增加供应商发货单字段。

#### 图标与颜色

- 导航、页面标题、状态、按钮和空状态可以使用风格统一的简洁图标；
- 图标用于辅助识别和适度装饰，关键操作不能只显示没有文字的图标；
- 图标旁保留短文字，陌生图标提供悬停说明；
- 绿色表示完成或正常，橙色表示待处理，红色表示退款、错误或高风险，蓝色表示信息和同步状态；
- 状态不能只依赖颜色区分，必须同时提供图标或文字；
- 装饰图标保持低干扰，不抢占金额、订单状态和主要操作的视觉注意力。

#### 阅读与操作舒适度

- 正文、表格和金额使用清晰易读的字号与行距，关键金额和待处理数量明显大于说明文字；
- 文字与背景保持足够对比度，禁用状态也必须可读；
- 按钮和可点击区域留出足够间距，手机端点击区域不小于 44 × 44 像素；
- 表单标签始终可见，错误提示紧邻对应输入项，并说明如何修正；
- 删除、覆盖导出等重要操作明确反馈结果；同步和导出过程中显示进度，不能让用户猜测是否生效；
- 页面刷新后保留合理的筛选条件，并清楚显示数据最近同步时间。

#### 人因验收

- 用户进入页面后，应在 5 秒内识别页面用途、最重要的数字和主要操作；
- 工作台首屏无需滚动即可看到待发货、担保中、退款售后和今日预计利润；
- 待发货页面无需进入详情即可识别商品、型号规格、数量和是否应发货；
- 利润统计无需进入订单详情即可看懂每笔订单的计算过程；
- 电脑常用宽度和手机宽度下均不得出现核心按钮被遮挡、字段重叠或必须横向滚动才能操作；
- 图标、颜色和状态文案在全站保持一致。

### 8.3 全部订单页面

“全部订单”是按业务状态组织的订单总览，不能只是把历史订单按时间混在一张长列表里。页面默认显示状态分类，不默认展示无分组的全部流水。

页面顶部用状态标签或状态卡显示订单数量，分类固定为：

1. 待发货；
2. 待完成；
3. 退款处理中；
4. 已完成；
5. 已退款；
6. 已关闭；
7. 待核对。

分类与展示规则：

- 每笔订单在同一时刻只能进入一个主要状态分类；退款处理中和已退款优先于原发货状态，避免同一订单在多个分类重复出现；
- 点击状态标签后只显示该类订单，标签同时显示该类订单数量；用户仍可选择“全部”，但每笔订单必须保留醒目的状态标签，并允许按状态分组；
- 待发货、待完成、退款处理中和待核对属于当前待办，默认将等待最久或最需要处理的订单排在前面；
- 已完成、已退款和已关闭属于已结束订单，默认按最近状态更新时间倒序排列；
- API 同步导致状态变化后，订单自动移入新分类，并刷新各分类数量；
- 支持按下单或付款时间段、订单号、商品类型、型号规格、供应商和收件人搜索筛选；清除筛选后回到默认状态分类；
- 订单行或卡片优先显示商品缩略图、型号规格、数量、实付金额、关键时间和当前状态，次要字段放入详情；
- 每个分类都有独立空状态，明确说明该状态当前没有订单，不能显示成数据加载失败；
- 从工作台、待发货、待完成、退款售后或利润统计下钻到全部订单时，自动带入对应状态和时间筛选条件。

全部订单页面用于跨状态查询和回溯；待发货、待完成与退款售后页面继续承担具体操作，避免在订单总览中堆叠大量操作按钮。

## 9. 发货长图要求

### 9.1 版式

- 适合手机查看和微信发送；
- 每位收件人的信息独立分块；
- 型号规格不得截断；
- 收件地址允许自动换行；
- 同一供应商只生成一张或一组连续图片；
- 页面显示生成时间、订单数和总件数；
- 底部提示供应商按收件人姓名和手机号后四位回复快递单号。

### 9.2 数据更新

生成前必须重新检查订单状态。导出完成后记录当时的订单快照。

如果导出后发生退款、取消、地址变化或规格变化：

- 工作台显示发货单已经过期；
- 明确列出发生变化的订单；
- 允许重新生成；
- 保留旧版，防止无法追溯供应商收到的内容。

### 9.3 隐私

发货长图包含手机号和地址，属于敏感经营数据：

- 仅登录后的店主可以生成；
- 默认只保存在本地私有目录；
- 不上传项目方服务器；
- 不写入普通日志或 Git 仓库；
- 页面预览可以遮挡部分手机号，最终导出显示供应商发货所需的完整信息；
- 支持设置自动删除过期导出图片，删除期限由用户决定。

## 10. 退款清单要求

退款清单可以按供应商导出长图，至少包含：

- 收件人姓名和手机号后四位；
- 商品型号规格和数量；
- 客户退款金额和当前售后状态；
- 用户填写成本形成的订单快照；
- 供应商待退成本金额（系统自动计算）；
- 是否需要退货；
- 退货单号；
- 申请时间和已等待时间；
- 处理备注。

已经确认从供应商收回的订单从当前待追回列表移出，但保留历史记录。

## 11. 数据接入和历史边界

- 接入闲管家 API 当日开始自动同步新订单及后续状态；
- 接入日前订单由用户按需手工导入；
- API 默认定时增量同步，也支持立即同步；
- 重复同步和重复导入不能产生重复订单；
- API 失败时保留已有数据并显示最后成功时间；
- 授权失效不影响在待发货页面查看历史发货单，也不影响在退款售后页面查看历史退款清单；
- 本地电脑关闭或休眠期间暂停同步，重新启动后补查平台允许查询的时间范围。

## 12. GitHub 本地发行和未来商业边界

### 12.1 当前 MVP 全部免费

当前版本的首要目标是获得第一批真实用户，验证待发货、退款、担保款和利润管理能否持续解决日常经营问题。本 PRD 列出的全部功能均免费提供，不设置 License、试用期限、付费墙、订单数量限制或到期停用：

- 闲管家 API 配置、自动同步和手工历史导入；
- 工作台、待发货、待完成、退款售后和全部订单；
- 发货长图与供应商退款清单导出；
- 担保中金额和平台参考回款日；
- 商品成本维护、逐单利润、每日预计利润和时间段实际利润；
- 本地备份、恢复和后续兼容升级。

用户从 GitHub 下载源码或官方发行包，在 Windows 本地安装和运行。首次访问创建自己的管理员账号，每个实例管理一个闲管家店铺。API 凭据、订单、地址、成本和导出文件保存在用户自己的电脑，经营功能不依赖项目方业务服务器。

发布包不得包含开发者 API 密钥、测试账号、真实订单、地址或手机号。闲管家 API 套餐和费用由用户直接向闲管家承担，以闲管家实际规则为准。

### 12.2 第一批用户原则

- 当前版本用完整可用的核心能力吸引用户，不用残缺功能迫使用户付费；
- 新用户不需要关注公众号、申请激活码或联系开发者即可使用本 PRD 的全部功能；
- 升级不能覆盖账号、订单、成本、发货记录和供应商映射；
- 用户反馈以自愿方式收集，未经同意不上传经营数据；
- 是否进入商业化阶段由实际用户规模、持续使用率和新增功能价值决定，不在当前 MVP 中预设收费时间；
- 当前 MVP 已经提供的能力保持为免费基础能力，未来收费对象是后续新增、更省时间或具有持续服务成本的增强功能。

### 12.3 未来 License 方案预留

用户规模形成后，可以为后续新增的多店铺、跨设备协作、更高级自动化、云备份或高级经营分析提供 Pro 模块。届时可以采用已经确定的公众号私发动态 Key 方案：

1. 用户在公众号私聊发送本地软件生成的店铺激活请求码；
2. 公众号只向该微信私发绑定微信与闲管家店铺的动态 Key；
3. 同一微信与店铺组合生成新 Key 后，上一枚未使用 Key 失效；
4. 新 Key 成功激活后，上一授权代次的 License 失效；
5. 动态 Key 限时且只能兑换一次，转发后不能用于其他微信或店铺；
6. 授权服务仅保存微信身份摘要、店铺摘要、授权模块和有效期，不保存订单、地址、成本、利润或闲管家 API 密钥。

这套 License、公众号回调、支付和 Pro 模块在当前 MVP 中只保留产品方向，不进入本阶段开发、导航、页面和验收范围。仓库代码许可证与未来 Pro 商业 License 是两种不同概念，公开发布前另行确定代码许可证。
## 13. MVP 不做

本版不做：

- 完整采购管理；
- 供应商应付账款；
- 逐单供应商付款登记；
- 完整库存和仓库管理；
- 采购在途、库龄和盘点；
- 包含工资、房租、税费和银行对账的完整会计总账、会计报表和税务核算；
- 客户画像和风险评分；
- 市场价格、新闻和商品生命周期分析；
- AI 客服和自动聊天；
- 自动向供应商发送微信；
- 自动审批退款；
- 多店铺、多租户和 SaaS 收费；
- 公众号动态 Key、License 服务、支付系统和 Pro 功能收费；
- 强制云服务器部署。

原 PRD 中与这些能力有关的内容保留在 Git 历史中，不再作为当前 MVP 的开发和验收要求。

## 14. 核心验收场景

### A1 已付款订单进入发货清单

给定一个有型号规格、数量和完整地址的已付款未发货订单，系统同步后进入待发货，生成长图时信息完整。

### A2 退款订单自动剔除

一个订单付款后进入待发货，但生成长图前客户申请退款。系统重新同步后将其从发货单剔除，并进入退款售后。

### A3 导出后发生退款

订单已经出现在发给供应商的长图中，随后客户退款。系统保留原导出记录，突出显示“联系供应商停止发货”，并允许生成更新后的清单。

### A4 已发货自动移出

闲管家出现发货时间和快递单号后，订单自动从供应商待发移入已发货待完成，卖家不重复填写单号。

### A5 未发货订单实时更新

每次自动同步后，API 当前显示为未发货的已付款订单都出现在待发列表中；API 显示整单已发货后自动移入已发货待完成。系统不拆分发货进度。

### A6 担保金额计算

三笔订单客户分别支付 100、200、300 元：100 元订单已经完成并转入商家账户，担保金额为 0；200 元订单已经全额退款，担保金额为 0；300 元订单仍已发货待完成，担保金额按客户实付计算为 300 元，不扣除 1.6% 平台费。退款完成或订单完成后自动移出担保计算，明细合计必须等于首页数字。

### A7 订单和预计回款时间

每笔订单显示准确的下单时间。正常快递订单按发货后第 10 天显示平台参考回款日；真实倒计时出现后覆盖参考日期，退款或客服介入后暂停参考。所有日期必须说明来源，不能把参考日期写成平台承诺。

### A8 供应商退款追回

客户退款后订单进入售后。用户已经为该商品类型填写成本时，系统使用本地保存的订单成本快照，自动计算并填入“供应商待退成本金额”；成本缺失时，用户只需补充一次该商品类型成本，所有匹配订单自动补算，不要求逐单填写金额。确认供应商退款后移出当前待退队列，历史仍可查询。

### A9 每日预计利润与时间段实际利润

商品 A 两件实付 200 元、单件成本 60 元。订单未完成时，预计平台费为 200 × 1.6% = 3.20 元，预计利润为 200 − 120 − 3.20 = 76.80 元，并归入付款日的每日预计利润；利润表必须逐列显示实付 200 元、成本“2 × 60 = 120 元”、平台费“200 × 1.6% = 3.20 元”和单笔利润 76.80 元。订单成功成交后，同一结果转为实际利润 76.80 元，并按 API 返回的 confirm_time 归入对应成交日期和所选时间段。商品 B 一件实付 100 元但尚未配置成本，系统明确显示一笔缺成本订单及其 100 元销售额，不把商品 B 计入预计利润。选择包含当天的时间段得到相同可复算结果；商品 A 后续全额退款时，其销售额、平台费和预计利润都回算为 0；如果尚未发货或没有运费损失，不需要填写任何费用，售后实际损失保持 0。只有用户确认实际承担了 12 元退货运费并填写后，系统才单独记录 12 元售后实际损失，不把该退款订单计入已成交订单的时间段实际利润。

### A10 本地发行

新用户从 GitHub 发行包安装，在自己的电脑创建账号并配置自己的 API。重启和升级后订单、地址、发货批次、退款记录、成本快照及供应商映射不丢失。

### A11 当前版本免费使用

新用户从 GitHub 下载并安装当前 MVP 后，无需关注公众号、输入激活码、购买 License 或等待人工开通，即可使用本 PRD 规定的全部功能。重启、升级和持续使用不会因试用期或订单数量触发功能锁定。

## 15. MVP 成功标准

- 卖家打开首页能在一分钟内知道今天需要发哪些订单；
- 发货长图包含地址和型号规格，不需要再从不同页面手工拼接；
- 已退款、关闭和已发货订单不会混入新的待发货清单；
- 供应商可以根据收件人姓名和手机号后四位回复快递单号；
- 所有待发订单均能追溯到最近平台状态和同步时间；
- 首页担保中金额能由订单明细复算；
- 每笔订单能看到下单时间、付款时间、发货时间、API 返回的成交完成时间和预计回款时间；
- 用户为每种商品类型填写一次成本后，系统能自动计算每笔订单利润；正常成交订单按客户实付金额的 1.6% 扣除平台费，全额退款订单的平台费为 0；
- 利润统计支持按开始日期和结束日期筛选，并提供今天、近 7 天、本月、上月和自定义快捷范围；筛选后汇总卡、趋势图、逐单明细、合计行与导出内容使用同一时间范围；每日预计利润由付款日订单的逐单利润汇总，时间段实际利润只由 confirm_time 落在区间内的已完成订单汇总；页面逐单显示完整计算式，顶部汇总、表格合计和逐单利润之和必须一致；缺少成本时不按零成本抬高利润；
- 退款订单不会因从发货列表消失而被遗忘；
- 日常正常订单无需手工登记供应商付款和卖家到账；
- 核心页面遵循第 8.2 节人因设计要求：图片优先辅助识别、图标风格统一、首屏层级清楚，并在电脑和手机宽度下无需横向滚动即可完成主要操作；全部订单默认按业务状态分类，每笔订单只进入一个主要分类，状态变化后自动移动并更新数量；
- API 暂时不可用时，历史数据和已经生成的清单仍可查看；当前 MVP 无需公众号、激活码或 License 即可使用全部规定功能，不设置试用期和订单数量限制。

## 16. API 依据与待验证事项

当前产品判断依据：

- [闲管家订单列表](https://s.apifox.cn/35842026-8755-4ac7-a61e-9b07fbcac599/api-95074856)：订单时间、付款、发货、确认、退款、收件信息、物流、商品规格和图片；
- [闲管家订单详情](https://s.apifox.cn/3ac13d69-5a38-4536-ae9b-a54001854ef8/api-93586385)：单笔订单完整字段，包括成交完成时间 confirm_time；
- [闲管家售后详情](https://s.apifox.cn/35842026-8755-4ac7-a61e-9b07fbcac599/api-472549188)：包含 timeout_time、timeout_status、timeout_type，但公开文档未说明完整字段语义，需用真实订单验证；
- [闲管家收货帮助](https://xygj.pro/website/content?article_id=500247675192645&category_id=230006&title=%E6%94%B6%E8%B4%A7&type=23)：普通快递 10 天后自动确认并打款；
- [闲鱼交易超时说明（闲管家规则页转载）](https://xygj.pro/website/content?article_id=498774171583877&category_id=220001&title=%E7%A4%BE%E5%8C%BA%E8%A7%84%E5%88%99&type=22)：普通快递等从发货起 10 天、买家可延长 3 天、退款或客服介入暂停倒计时；特殊订单以订单页为准。

开发前优先用真实订单验证：

1. 已付款待发、已发货、完成、关闭各状态代码，以及如何可靠排除未付款订单；
2. 退款申请中、已退款和退货退款各状态代码；
3. 隐私订单的收件人、电话和地址是否完整；
4. timeout_time、timeout_status、timeout_type 对应的售后阶段、是否仍在倒计时以及暂停后的返回值；
5. 真实退款完成状态与 refund_amount、refund_time 的对应关系，确保钱实际退回客户后立即扣减。


未验证项进入待核对，不阻塞已证明字段的只读同步和发货清单生成。
