# M1 业务规则与卡点确认

> 版本：V0.2  
> 日期：2026-09-06  
> 状态：第一批阻塞问题已确认；核心业务规则包待评审  
> 目标：只确认会阻塞技术设计或核心数据模型的问题；其余问题按里程碑延后确认

## 1. 已消除的卡点

### 1.1 店铺范围

- 第一版只管理一个鱼小铺。
- 不提供闲鱼店铺切换、多店铺权限和跨店铺报表。
- 数据模型保留店铺归属字段，避免未来扩展时进行破坏性改造，但第一版界面不暴露多店铺复杂度。

### 1.2 履约来源

- 商品以自有库存为主。
- 闲鱼订单付款后优先锁定现有库存。
- 非闲鱼渠道订单确认后锁定现有库存。
- 库存不足时才进入补货采购；付款后采购和供应商直发作为补充流程。
- 第一版必须优先保证锁定、释放、出库、退货入库和盘点流水正确。

### 1.3 外部接口依赖

- 闲管家 API 不作为开工或核心系统上线的前置条件。
- 先实现手工录入和 CSV/XLSX 增量导入。
- API 通过适配层后接，故障时不得影响已有订单、库存和资金操作。

## 2. 第一批阻塞问题（已确认）

2026-09-06，产品负责人确认以下四项全部采用推荐方案。

### 2.1 部署与手机访问

推荐方案：使用一台云服务器部署响应式网页，电脑和手机通过浏览器访问；视频保存到私有对象存储，通过短时授权链接访问。

原因：发货时需要手机扫码上传视频，云端访问最省事，也便于备份和后续多人使用。代价是会产生服务器、域名、HTTPS 和对象存储费用，并需要做好登录与隐私保护。

确认结果：采用云端部署并允许手机通过安全登录或短时授权页面访问。

### 2.2 第一版使用人员

推荐方案：第一版实现管理员和操作员两种角色。初期可以只有管理员账号；操作员用于未来打包或客服人员，只能处理订单、库存和取证，不能查看密钥或进行高风险系统设置。

原因：预留两个基础角色成本较低，可以避免未来通过共享管理员密码协作；无需第一版实现复杂的角色自定义。

确认结果：按“管理员 + 操作员”两个基础角色设计。

### 2.3 利润、回款与现金占用口径

推荐方案：将三类指标分开，避免混为一个“赚了多少”。

- 预估利润：订单应收金额减去预计全部成本，用于订单尚未完成时判断是否值得交易。
- 已实现利润：订单履约完成后，以实际净收款减去实际成本、费用和售后损失计算。
- 资金占用/现金流：已采购成本、已发货未回款、已签收待确认和渠道待收款单独展示，不称为利润。

部分收款订单在收齐前不计入完整的已实现利润；可以显示“已收金额”和“待收金额”。历史成本采用订单成本快照，供应商后续调价不追改历史利润。

确认结果：采用预估利润、已实现利润和资金占用/现金流三类口径。

### 2.4 库存锁定时点

推荐方案：

- 闲鱼拍下未付款不锁库存，付款成功后锁定。
- 非闲鱼渠道的草稿订单不锁库存，用户点击“确认订单”后锁定。
- 取消或关闭订单立即释放锁定库存。
- 发货、自提或线下交付完成后，将锁定库存转为正式出库。
- 退货不自动回补库存，必须在确认收到且检查可再次销售后入库。
- 库存不足时允许订单进入“缺货待采购”，但必须醒目标记，不静默产生负库存。

确认结果：采用以上库存锁定、释放、出库和退货回补规则。

## 3. 推荐直接采用的默认规则

以下事项不必单独阻塞讨论，若产品负责人没有异议则直接写入后续技术设计：

- 金额使用人民币分或精确小数保存，不使用浮点数。
- 系统时间按 Asia/Shanghai 展示，数据库保存明确时区。
- 运费、包装费和渠道费用支持全局默认值与订单覆盖值；商品采购成本优先来自实际库存成本或采购单。
- 已完成订单的金额、成本和利润如需修改，必须记录修改前后值、原因、时间和操作人。
- 同一业务事件重复提交时只产生一次订单、库存流水和资金流水。
- 客户跨渠道合并只允许用户确认，不根据昵称自动合并。
- 未留取证视频只提醒，不阻止发货。
- 高风险客户、降价风险和补货建议只辅助决策，不自动执行拉黑、改价、下架或采购。

## 4. 本批确认后的产物

四项阻塞问题确认后，Codex 将继续完成：

1. 闲鱼订单、渠道订单、售后和收款状态字典。
2. 库存锁定、释放、出库、退货和盘点规则表。
3. 采购、资金和利润计算口径表。
4. 核心业务流程及异常分支。
5. P0 验收用例。

上述 M1 业务规则包完成并检查一致性后，直接进入 M2 技术实现文档，不设置无必要的等待时间。
