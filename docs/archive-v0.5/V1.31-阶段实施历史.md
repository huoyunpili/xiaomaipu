## 本轮检查记录（2026-09-15）

- 已修复 3 处 mypy 类型错误和 settings 格式问题；Ruff、格式、mypy、Django check、迁移漂移检查通过。
- 首次全量测试：135 通过、11 失败。3 项仍依赖旧首页内容，8 项浏览器测试因缺少 Playwright Chromium 可执行文件而未运行成功；均保留为未通过。
- 发现并修复首页担保下钻遗漏未发货/退款风险订单、仅带 export 参数时丢失已保存利润筛选；新增两项回归测试并通过。
- 技术手册已补充调用链、字段归属、状态与事务契约、接口、隐私和发行验证要求。
- 使用本机 Chrome 运行 `tests/test_workspace.py` 和 `tests/test_workspace_browser.py`：11 项通过（19.34 秒）。七个页面在 1365×900 和 390×844 下无横向溢出、无 JS 异常；PNG 下载签名正确。该浏览器用例主要验证页面访问和布局，不代替全部业务交互验收。
- 已补齐发货 PNG 总件数和供应商回复提示；人工查看 `artifacts/browser/workspace/shipping.png`，确认中文长地址完整换行、总数和页码可见。另查看手机利润页截图，空状态正常，但合计区手机字段标签仍待改善。
- 浏览器运行命令：先设置 `$env:PLAYWRIGHT_CHANNEL='chrome'`，再执行 `.venv\Scripts\python.exe -m pytest tests/test_workspace.py tests/test_workspace_browser.py -q --tb=short`。此前 8 项旧浏览器测试仍需在该环境下重跑，不能沿用新测试结果认定通过。

## 后续实施记录（2026-09-15）

- 新增 `CostVersion` 和 `0002_cost_versions`，保存成本版本、生效时间；未付款订单不再提前取成本，晚同步订单按付款时间匹配。已有库只回填可证实的当前版本，不伪造旧价格时间。
- 利润页补齐六项汇总、利润/商品排序、运费损失列、金额来源展开、正负利润趋势及手机合计标签；CSV 包含相同区间的逐单、合计与单列售后损失，时间使用 +08:00。
- 全部订单待办按付款等待时间正序、已结束按更新时间倒序；待完成支持参考回款日排序；担保下钻和售后历史筛选在再次查询时保留。
- 创建清单时重新锁定并读取所选订单，防止旧内存对象把已退款订单带入发货批次；历史批次页列出与快照不同的具体订单。
- 新增排序/汇总/导出一致性、北京时间月初月末边界、售后损失单列、过期对象导出、成本版本及订单排序测试。
- 全量复核为 153 通过、1 个新浏览器定位失败；修正排序控件标签匹配后，单独重跑新版浏览器用例通过（20.39 秒）。此前旧版浏览器及全部业务/安全测试已通过，未移除业务断言。
- 人工查看新版利润页桌面和手机截图：200.00 − 120.00 − 3.20 = 76.80，六项汇总与合计一致；手机无横向溢出，合计显示字段标签。截图在 `artifacts/browser/workspace/`，均使用合成订单。
- 最终完整复跑：设置 `PLAYWRIGHT_CHANNEL=chrome` 后执行 `scripts/check.py`，退出码 0；154 项测试通过，耗时 151.09 秒。完整输出保存于 `artifacts/verification-v131-complete.txt`。本记录证明当前自动化门禁通过，不证明尚未覆盖的 API 真实语义、私有文件清理或新版安装恢复已经完成。

## 接入和私有清单实施记录（2026-09-15）

- 官方 OpenAPI 明确订单 23 为已退款、24 为已关闭；本机 78 条真实快照中 13 条全额退款同时满足代码、时间和金额证据。修正退款关闭、未知值、退款完成及矛盾字段处理，证据见 API 字段核验文档。
- API/CSV 双向复用商品成本；缺少 SKU 且匹配不唯一时要求可选规格标识，保持旧模板兼容。历史导入订单在后续 API 出现时按同号关联，保留成本快照。
- 售后详情可以先于订单列表触发全额退款投影；有版本响应保留乱序保护，无版本详情不伪造 update_time。发货后空收件字段不抹掉本地已有地址。
- 新增 `0003_refund_facts`、`0004_private_export_images`。退款长图包含退款金额、成本、退货要求、单号、申请时间及等待时长；人工查看 `artifacts/browser/workspace/refund.png`，信息完整，电话仅显示后四位。
- PNG 通过本地 CSRF 接口保存在私有目录，服务端检查格式和大小，下载要求店主登录；图片默认永久保留，用户设定天数后每日清理过期图片。清理测试确认原批次快照仍在，旧图能重新生成。
- 最终执行 `PLAYWRIGHT_CHANNEL=chrome` 下的 `scripts/check.py`：退出码 0，172 项测试通过（151.61 秒），Ruff、格式、mypy、Django 与迁移检查均通过。输出：`artifacts/verification-v131-refund-storage.txt`。测试和浏览器图片使用合成订单及临时私有目录，没有修改真实经营数据。

下一步：补完供应商微信备注/默认发货备注和统一参考日设置、完整售后追回与回款下钻交互、跨店和发布包隐私专项，再做新版升级和恢复演练。API 未取得的真实状态转移继续保留限制说明；不以自动化绿色结果代替全部 PRD 验收。

## 回款设置与下钻实施记录（2026-09-16）

- 已增加统一参考天数设置，默认 10 天，以 24 小时为一天；自定义规则明确标为经营参考。仅店主可修改，保留前后值审计，图片保留设置互不覆盖。
- 首页今日、未来三天（含今日）、超过参考时点均链接对应待完成明细；退款风险链接仍在担保中的退款订单。筛选、分页和 CSV 保留回款范围。
- 新增北京时间零点、当天当前时刻、三天窗口边界测试，验证首页金额、明细和 CSV 集合一致；退款与已结束订单排除，规则修改和输入边界验证通过。
- 首轮专项 33 项通过、2 项失败：旧利润测试将付款时间设为当前时间减 90 秒，在本次午夜执行时跨到昨日。已修改公共合成订单默认付款时间为当前时间，消除该数据与“今日”断言的不一致。
- 完整门禁退出码 0：174 项测试通过（157.35 秒），Ruff、格式、mypy、Django 和迁移检查通过；日志 `artifacts/verification-v131-repayment.txt`。人工查看手机设置截图，统一回款规则及独立图片保留表单完整、无横向溢出。随后发现并修正下钻说明的中文编码，相关页面另行复核。
- 已修复发布包脚本敏感文件正则的多余转义；14 个文件名样例验证密钥、环境配置、日志、备份受到拦截，`.env.example` 和正常源码允许。此项不代表完整发行包已检查或发布。
- 中文说明修正后，回款边界/下钻及新版浏览器两项复测通过（22.11 秒），日志 `artifacts/verification-v131-repayment-ui.txt`。
- 后续仍需供应商微信备注/默认发货备注、售后追回交互、跨店隐私与发行升级恢复验证。

## 商品备注与售后操作实施记录（2026-09-16）

- 新增商品及订单的供应商微信备注、默认发货备注和独立平台备注。API/CSV 复用商品配置；本单备注不再被 API 卖家备注覆盖。当前待发货订单跟随默认备注更新，已发货记录保持原值。
- 修正编辑订单时无条件设置供应商覆盖标志的问题：只有修改本单供应商或微信备注才停止跟随默认映射。仅改商品备注或供应商不生成新成本版本。
- 发货清单合并三种备注，更新后旧批次提示过期，原快照保持不变。旧库不推断备注来源，已有内容全部保留。
- 专项回归 37 项通过（16.75 秒），记录 `artifacts/verification-v131-notes-targeted.txt`。浏览器新增商品配置表单及确认追回、历史查看、撤销确认流程；全量门禁正在执行。
- 未完成范围仍包括跨店安全、完整 A1–A11 核对、发行包及新增模型升级恢复实测。
- 全量初次运行：175 通过、1 个浏览器用例失败，原因是新增测试在 Playwright 事件循环中直接执行同步 ORM。修正为页面验证保存结果，退出浏览器上下文后再查询数据库；单独复跑通过（23.50 秒）。人工查看手机商品配置和已追回历史截图，字段可读、无横向溢出。
- 追回审计增加修改后状态，与修改前状态共同记录；浏览器流程增加确认与撤销两个审计事件的断言。
- 最终完整执行 `scripts/check.py` 退出码 0，176 项测试通过（158.60 秒），Ruff、格式、mypy、Django 与迁移检查通过。记录 `artifacts/verification-v131-product-notes-final.txt`。后续进入跨店隔离、发行包隐私和升级恢复演练，并逐项闭合 A1–A11。

## 店铺数据隔离实施记录（2026-09-16）

- 发现并修复商品配置全局共用的问题：增加店铺归属及 `(shop, key)` 唯一约束，API/CSV 商品解析、成本设置和订单回填均限定店铺。
- 新增 `0007_product_shop`：根据订单关联分配旧配置；共享配置复制到各店铺并复制成本版本与生效时间，订单成本快照保持不变。无可证明归属的配置保留为未归属数据，不向当前店铺展示。回退需恢复备份，不提供有损的逆向合并。
- 39 项专项测试通过（17.25 秒），日志 `artifacts/verification-v131-isolation-targeted.txt`。测试覆盖同商品标识跨店不同成本、页面内容不可见、越界读写 404、越界导出不调用刷新、私有 PNG 不可下载，以及旧共享配置拆分后订单金额/版本保留。
- 数据拆分测试验证迁移函数在旧关联结构上的结果；完整旧版数据库升级和备份恢复仍须独立演练，不能以该测试替代。
- 完整门禁正在执行；后续继续日志/发行包隐私、升级恢复与 A1–A11 逐项验收。
- 补充 `.gitignore`、`.gitattributes` 和 `.dockerignore` 的敏感文件及运行目录排除；文件名样例经 `git check-ignore` 和 `git check-attr` 验证生效。最终归档和镜像内容仍待正式打包检查。
- 修复本地发行 Gunicorn 默认访问日志记录完整请求地址的问题，改为仅输出状态码与耗时。应用 `SafeJsonFormatter` 原有敏感文本排除测试继续保留。使用合成环境配置执行 `docker compose -f deployment/compose.local.yaml config --quiet` 验证配置；真实运行日志将在发行演练中复核。
- 完整门禁退出码 0：178 项测试通过（161.65 秒），Ruff、格式、mypy、Django 和迁移检查通过。日志 `artifacts/verification-v131-isolation.txt`。本轮真实经营库未迁移；下一步在独立环境验证升级、内容级恢复和发行材料。

## 升级恢复实施记录（2026-09-16）

- 新增可复现隔离演练脚本，实际创建旧结构并写入旧商品、销售订单、描述快照，经过 `0006` 和当前迁移，验证跨店拆分、成本版本、订单快照和历史清单保留。
- 实际运行 `backup_local --verify` 与独立 `pg_dump`/`pg_restore`。65 张表的内容摘要及一张私有 PNG 哈希全部一致，重复迁移无数据改写；报告 `artifacts/restore/92caf8fd92f4/report.json`。
- 备份校验新增逐表内容摘要，弥补行数相同但字段损坏无法发现的缺口；新增命令 `fingerprint_database`，只输出摘要。详细流程与边界见 [升级恢复执行记录](docs/V1.31-升级恢复执行记录.md)。
- 首次演练发现 PostgreSQL 排序表达式引用别名不成立，已修正并重跑通过；失败临时库由脚本清理，未重用失败结果。
- 尚需把内容校验接入发行 PowerShell 管理脚本，完成最终发行包、独立安装和运行日志验证。当前演练不代替这些交付。
- 新增内容损坏回归用例通过：行数相同、字段值改变时摘要不同；恢复原值后摘要相同，输出不包含原始字段内容。
- 最终完整门禁退出码 0，179 项测试通过（159.69 秒），静态、Django 和迁移检查全部通过；日志 `artifacts/verification-v131-backup-final.txt`。只读查询确认本轮 `seller_wb_probe_` 临时数据库数量为 0。

## 发行管理流程记录（2026-09-16）

- 0.6.0 RC1 镜像构建、新建独立数据目录安装和全部迁移通过。首次自动备份遇到 PowerShell 短参数冲突，修复后 Start、Backup、Restore、Upgrade 均以退出码 0 完成。
- 首次店主建号及入口关闭通过；10 个桌面/手机页面无横向溢出或 JS 错误，运行访问日志不含合成搜索标记。
- 含工作台订单、成本版本、供应商备注、PNG 和私有文件的覆盖恢复验证通过，恢复后的真实发行实例仍可登录。升级后字段保持原值。
- 内容校验辅助程序在旧 0.5.0 镜像中运行成功；新镜像源码逐文件匹配及配置隐私检查通过。项目版本和锁文件统一为 0.6.0，离线锁检查通过。
- 本轮完整代码门禁通过，179 项测试（218.97 秒）；之后的 PowerShell 参数修正通过实际管理动作验证。详细证据见 [发行安装执行记录](docs/V1.31-发行安装执行记录.md)。
- 源码审查包已在独立提交副本生成：414 个条目，必要文件、路径及配置隐私检查通过，原工作区 Git 状态不变。新镜像匹配 248 个源码文件，扫描 255 个项目文件通过。
- 完整 Stop→Start 退出码均为 0，停启后业务字段和私有文件校验通过。报告及包摘要见发行安装记录。
- 下一步：A1–A11 完整验收、修正发现的差异，并刷新最终交付包与文档。
