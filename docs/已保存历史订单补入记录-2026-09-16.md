# 已保存历史订单补入记录

按用户「先把能查到的补进去」的要求，2026-09-16 15:10 将当前店铺已保存的平台历史订单补入工作台。

- 新增 31 单，下单日期为北京时间 2026-08-29 至 2026-09-10。
- 状态：已完成 17、已退款 8、已关闭 3、待核对 3。
- 后台从 49 单增加至 80 单；已保存的平台订单均已纳入。
- 原有 49 单逐字段核验未变；重复执行新增 0 单。
- 保留平台原始范围和 2026-09-11 同步起始日，新增来源为 `API_HISTORY`。
- 不完整的退款事实维持待核对，不计入正常回款、担保或利润；缺失成本不按零成本计算利润。
- 本次只使用本地已保存记录，不能据此宣称更早历史已全部查全。

执行前使用 `backup_local` 备份。导入通过事务执行并记录 `workbench.saved_history_imported` 审计事件；私有汇总记录在 `.local/saved-history-import-report.json`。没有执行平台退款、确认收货、发货或资金操作。

新增 `import_saved_history` 服务支持按店铺连接补入、去重及权限检查，默认自动同步规则不变。历史补入、跨期统计与工作台相关测试 22 项通过；Ruff 与修改文件的类型检查通过。
