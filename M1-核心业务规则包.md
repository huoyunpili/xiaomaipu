# M1 核心业务规则包

> 版本：V0.2  
> 日期：2026-09-07  
> 状态：已通过，作为 M2 技术设计输入  
> 适用范围：闲鱼小卖家后台管理系统 MVP  
> 上游依据：《PRD-闲鱼小卖家后台管理系统-MVP.md》《M1-业务规则与卡点确认.md》

## 1. 文档目的

本规则包把 PRD 转换为稳定、可测试的业务规则，作为 M2 技术设计的数据模型、状态机、接口和测试依据。

本阶段只定义“业务上必须怎样运行”，不提前锁死编程语言、框架、数据库品牌或云服务商。

## 2. 核心设计原则

1. **多维状态分离**：订单履约、收款、库存、售后和取证分别保存，不用一个“订单状态”表达全部情况。
2. **事件驱动留痕**：状态变化既更新当前状态，也保存事件或流水，能够回答何时、为何、由谁改变。
3. **共享经营底账**：闲鱼与非闲鱼渠道使用同一套 SKU、库存、采购、客户、资金和利润规则。
4. **自有库存优先**：付款或确认订单后优先锁定现货，不足部分明确进入缺货待采购。
5. **账实分开**：应收、实际收款、利润和现金占用分开统计。
6. **历史快照**：订单成交时使用的商品、价格和成本保留快照，后续资料变化不反写历史。
7. **默认不做高影响自动操作**：不自动拉黑、改价、下架、取消采购或把退货直接变为可售库存。
8. **所有入口幂等**：API、导入、手工重试和页面重复点击不得造成重复订单、重复扣库存或重复记账。
9. **小卖家轻量操作**：低频库存和报价变化优先快捷手工维护，批量导入只做初始化与兜底；不引入发票、税务、多仓和复杂仓储流程。

## 3. 统一术语

| 术语 | 业务定义 |
|---|---|
| 销售渠道 | 订单来源，如闲鱼、微信熟客、转介绍、线下或其他平台 |
| 平台订单号 | 闲鱼或其他平台生成的外部订单标识 |
| 内部订单号 | 系统为每笔销售订单生成的唯一标识，所有渠道都必须具备 |
| 订单确认 | 非闲鱼草稿订单转为正式履约订单的动作，确认后尝试锁定库存 |
| 实际库存 | 已入库、归系统管理且尚未正式出库的数量，不含在途和退货待检 |
| 锁定库存 | 已被正式订单占用、尚未正式出库的数量 |
| 可售库存 | 实际库存减去锁定库存，不得小于零 |
| 在途库存 | 已下采购单且供应商已发货、尚未验收入库的数量 |
| 待检库存 | 客户退回或来源待确认、尚未判断能否再次销售的数量 |
| 缺货数量 | 订单需求数量减去已锁定数量，不得小于零 |
| 应收金额 | 按订单成交约定应向客户或平台收取的净金额 |
| 实际收款 | 已确认到账的收款或平台结算金额 |
| 待收金额 | 应收金额减去有效收款，再加已发生但尚未冲销的调整 |
| 预估利润 | 应收金额减去预计商品成本和预计费用 |
| 已实现利润 | 履约完成后的实际净收款减去实际商品成本、实际费用和售后损失 |
| 资金占用 | 已支付但尚未通过回款释放的采购、库存或履约资金，不等同于亏损 |
| 成本快照 | 订单确认、库存分配或采购完成时固化到订单上的历史成本依据 |
| 业务事件 | 下单、付款、锁定、出库、收款、退款等不可省略的历史记录 |

## 4. 订单统一模型

### 4.1 订单必须分开的五个维度

每笔订单至少包含以下五组互不覆盖的状态：

1. **来源渠道**：闲鱼、微信熟客、转介绍、线下、其他平台或自定义渠道。
2. **履约状态**：是否需要备货、发货、交付以及是否完成。
3. **收款状态**：客户是否付款、平台是否结算、是否退款或坏账。
4. **库存状态**：未锁定、已锁定、缺货、已出库、退货待检或已回补。
5. **售后状态**：无售后、申请中、退货中、处理完成或纠纷中。

取证状态作为第六个辅助维度独立保存：无需取证、未留证、上传中、已留证、上传失败或文件异常。

### 4.2 订单身份与防重

- 所有订单使用系统生成的内部订单 ID 和内部订单号。
- 闲鱼订单以“店铺 ID + 平台订单号”作为业务唯一键。
- 其他平台存在稳定订单号时，以“渠道 ID + 外部订单号”作为业务唯一键。
- 微信、线下等没有外部订单号时，只使用内部订单号；导入模板应携带上次导出的内部订单号以便更新。
- API 事件还需保存外部事件 ID；没有事件 ID 时使用订单、事件类型、外部更新时间和内容摘要构造幂等键。
- 重复请求返回原处理结果，不重复创建业务记录。

## 5. 闲鱼订单状态规则

### 5.1 履约主状态

| 状态 | 进入条件 | 允许的主要动作 | 正常下一状态 |
|---|---|---|---|
| 拍下未付款 | 获取到已下单但未付款记录 | 查看、备注、等待、关闭 | 已付款待备货、交易关闭 |
| 已付款待备货 | 平台确认买家付款 | 自动尝试锁库存、关联采购 | 已备货待发货、缺货待采购 |
| 缺货待采购 | 需求量未被库存完全满足 | 创建采购、等待入库、取消 | 已备货待发货、交易关闭 |
| 已备货待发货 | 所有需要发货的数量已锁定或已确认直发 | 上传取证、填写物流、发货 | 已发货运输中 |
| 已发货运输中 | 已确认发货并保存物流信息或直发信息 | 查看物流、处理售后 | 已签收待确认、交易成功 |
| 已签收待确认 | 已知物流签收但平台尚未确认交易成功 | 提醒关注、处理售后 | 交易成功 |
| 交易成功 | 平台确认交易完成 | 核对结算、归档、售后 | 终态；售后维度可继续变化 |
| 交易关闭 | 未付款关闭、取消或其他平台关闭结果 | 释放锁定、记录原因 | 终态 |
| 待人工确认 | 外部状态缺失、冲突或无法映射 | 人工核对与修正 | 任一合法状态 |

### 5.2 闲鱼收款状态

| 状态 | 说明 |
|---|---|
| 未付款 | 买家尚未向平台付款 |
| 平台托管待结算 | 买家已付款，但资金尚未实际结算给卖家 |
| 结算中 | 平台显示交易完成，但到账结果尚未确认 |
| 已回款 | 已确认平台结算或实际到账 |
| 部分退款 | 已发生部分退款，仍保留部分净收款 |
| 已退款 | 已全额退款或净收款为零 |
| 冻结/风险 | 资金因退款、纠纷或平台处理存在不确定性 |
| 待人工确认 | 接口或导入信息不足，不能判断实际回款 |

规则：买家在闲鱼付款只能进入“平台托管待结算”，不能直接计为卖家已回款。

## 6. 非闲鱼渠道订单状态规则

### 6.1 履约状态

| 状态 | 进入条件 | 库存影响 |
|---|---|---|
| 草稿 | 快捷开单尚未确认 | 不锁库存 |
| 已确认待备货 | 用户确认正式订单 | 尝试锁库存 |
| 缺货待采购 | 库存不足 | 锁定已有部分，缺口单独记录 |
| 已备货待交付 | 库存已满足或直发已落实 | 保持锁定 |
| 已发货/配送中 | 快递或同城配送已经发出 | 锁定转正式出库 |
| 待自提 | 商品已备好，等待客户自提 | 保持锁定 |
| 已完成 | 自提、交付或无需物流的服务完成 | 锁定转正式出库；已发货订单确认履约完成 |
| 已取消 | 用户确认取消 | 释放未出库的锁定库存 |
| 售后处理中 | 已交付后发生退换、补发或退款 | 不自动回补库存 |

### 6.2 收款状态

| 状态 | 计算规则 |
|---|---|
| 未收款 | 有效收款合计为 0，且应收金额大于 0 |
| 部分收款 | 有效净收款大于 0 且小于应收金额 |
| 已收款 | 有效净收款等于应收金额 |
| 超额收款待处理 | 有效净收款大于应收金额，必须人工确认退款或调整 |
| 部分退款 | 已发生退款但净收款仍大于 0 |
| 已退款 | 净收款为 0 且存在退款记录 |
| 坏账/损失 | 用户明确将剩余应收核销为无法收回 |
| 待人工确认 | 收款与订单金额冲突或导入数据不足 |

规则：履约完成和收款完成相互独立。允许“已完成但未收款”，也允许“已收款但尚未交付”，两者都必须在工作台显示。

## 7. 售后状态规则

售后状态不得覆盖订单履约状态：

| 状态 | 说明 |
|---|---|
| 无售后 | 当前没有进行中的售后 |
| 退款申请中 | 客户要求退款，尚未决定或完成 |
| 待客户退货 | 已同意退货，等待客户寄回 |
| 客户已退货 | 客户已寄出，尚未验收 |
| 退货待检 | 已收到退货，尚未确定可售、维修或报废 |
| 待卖家处理 | 需要退款、换货、补发或拒绝处理 |
| 纠纷处理中 | 平台、支付渠道或双方争议尚未结束 |
| 售后完成 | 本次售后已形成明确结果 |
| 售后关闭/拒绝 | 申请撤回、拒绝或无需继续处理 |

一笔订单可有多次售后记录，但同一售后申请只能有一个当前状态。每次退款、退货、换货或补发均以独立业务记录和流水处理。

## 8. 库存规则

### 8.1 库存数量关系

- `可售库存 = 实际库存 - 锁定库存`
- `缺货数量 = max(订单需求数量 - 已锁定数量 - 已落实直发数量, 0)`
- 在途库存和待检库存不计入可售库存。
- 正常业务操作不得使实际库存、锁定库存或可售库存为负数。
- 导入发现历史负库存时，不伪造入库记录；生成库存异常待办，由管理员核对期初库存或遗漏流水。

### 8.2 锁定规则

- 闲鱼拍下未付款不锁库存，平台确认付款后锁定。
- 非闲鱼草稿不锁库存，点击确认订单后锁定。
- 同一事务中检查可售量并完成锁定，避免两个订单同时占用同一件库存。
- 库存足够时全额锁定；库存不足时只锁定实际可用数量，剩余数量明确标记缺货。
- 重复付款推送、重复确认或页面重试不得增加第二次锁定。
- 订单取消、关闭或商品明细减少时，释放尚未出库的相应锁定量。

### 8.3 出库规则

- 发货、自提确认或线下交付时执行正式出库。
- 出库同时减少实际库存和锁定库存，保证可售库存不因出库动作再次减少。
- 未锁定的订单不得直接正常出库；管理员特殊处理必须先补做库存分配或记录库存异常原因。
- 一个订单分批发货时按实际发货数量分批出库，并保留每批物流和时间。
- 供应商直发不改变自有库存，但必须关联采购成本和直发履约记录。

### 8.4 退货规则

- 客户发起退货不改变库存。
- 收到退货后进入待检库存，不进入可售库存。
- 验收结果为可再次销售时，生成退货入库流水并增加实际库存。
- 需要维修、降级销售或报废时，记录处理结果、预计损失和后续去向。
- 换货按“退回商品待检 + 新商品重新锁定和出库”两条独立流水处理。

### 8.5 盘点规则

- 盘点差异不得直接覆盖原库存，必须生成盘盈或盘亏流水。
- 盘点记录包含账面数量、实盘数量、差异、原因、时间和操作人。
- 已被订单锁定的库存仍参与实盘；发现实物缺失时对应订单进入库存异常待办。
- MVP 不建立盘点审批流；调整前展示变化并要求填写原因，保存后保留流水和操作记录。

### 8.6 库存成本规则

- 每次采购入库形成库存批次，分别保留供应商、成色、数量、商品采购单价和分摊后的采购运费。
- 默认按先进先出分配库存批次；对于一物一况或成本差异明显的商品，允许用户指定实际批次。
- 货况以自由描述为主，成色、配件、瑕疵和检测说明选填，支持自定义标签与照片；内部建档不要求全新、正品认证或配件齐全，留空不代表无瑕疵。
- 同款不同货况分别保存为实物/货况组，底层沿用批次，不要求每种瑕疵新建 SKU。一物一况商品锁定时必须选择实际货物，不自动跨货况替换；同货况普通库存继续先进先出。
- 订单保留成交货况，并在分配/发货时按实际批次保存货况快照；后续修改货盘不覆盖历史记录。内部备注不带入对外商品说明。
- 发货时把实际分配批次的商品采购成本和采购运费分摊分别固化到订单成本快照，计算利润时不得重复计入。
- 尚未确定实际批次时可显示预计成本，但订单完成前必须形成实际成本或明确的人工成本。
- 供应商后续报价只影响新采购建议，不改变历史库存批次和已完成订单成本。
- 库存批次主要由系统根据采购入库自动生成和分配；除一物一况或成本差异明显的商品外，用户日常只维护库存数量，不需要手工操作批次。

## 9. 采购规则

- 采购分为库存补货、订单缺货采购和供应商直发三种目的。
- 采购单状态为待下单、已下单、供应商已发货、部分到货、已到货、已取消或已退款。
- 创建采购单只增加计划数量，不增加实际库存。
- 供应商发货后增加在途库存；验收入库后减少在途并增加实际库存。
- 部分到货按实际数量入库，未到数量继续保留在途或异常。
- 采购退货或供应商退款必须生成反向库存/资金记录，不删除原采购。
- 多笔订单可以关联同一补货采购，但不得把同一采购成本重复分摊给多个订单。

## 10. 收款、退款与利润规则

### 10.1 金额字段

每笔订单至少保存：

- 商品成交金额。
- 客户承担运费或其他加收金额。
- 优惠与抹零。
- 应收金额。
- 实际收款合计。
- 退款合计。
- 待收金额。
- 商品成本。
- 采购运费分摊。
- 发货运费。
- 包装费。
- 平台或渠道费用。
- 其他履约费用。
- 售后损失与可追回金额。

金额统一使用人民币分或数据库精确小数，界面显示到元；禁止使用二进制浮点数参与结算。

### 10.2 基础公式

- `应收金额 = 商品成交金额 + 客户承担费用 - 优惠与抹零`
- `调整后应收金额 = 应收金额 - 已确认退款对应的订单减免金额 + 其他经审计的应收调整`
- `实际净收款 = 有效收款合计 - 已支付退款合计`
- `待收金额 = max(调整后应收金额 - 实际净收款 - 已核销坏账, 0)`
- `预计总成本 = 预计商品成本 + 预计采购运费 + 预计发货运费 + 预计包装费 + 预计平台/渠道费用 + 预计其他费用`
- `预估利润 = 应收金额 - 预计总成本`
- `实际总成本 = 商品成本快照 + 采购运费分摊 + 发货运费 + 包装费 + 平台/渠道费用 + 其他履约费用 + 售后损失`
- `已实现利润 = 实际净收款 - 实际总成本 + 可追回金额`

如果退款已经从实际净收款中扣除，不得再次作为成本重复扣减。退款产生的额外运费、货损或手续费计入售后损失。

### 10.3 已实现利润确认条件

- 闲鱼订单：履约状态为交易成功，实际结算可确认，实际成本已形成。
- 非闲鱼订单：履约已完成，订单应收已收齐或剩余金额已明确核销，实际成本已形成。
- 未满足条件时只展示预估利润、已收金额和待收金额，不把完整利润计入已实现利润汇总。
- 发生后续售后时允许重算已实现利润，但保留重算前后值和触发事件。

### 10.4 资金占用分类

工作台分别显示以下互斥或可核对的资金篮子：

- 在库资金：当前实际库存对应的剩余成本。
- 在途采购资金：已付款采购中尚未入库的成本。
- 已锁定待发货成本：已分配给订单但尚未出库的库存成本。
- 已发货未回款成本：已经出库但实际回款未确认的订单成本。
- 已签收待确认金额：闲鱼已签收但尚未结算的应收金额。
- 渠道待收款：非闲鱼订单尚未收回的余额。
- 售后风险金额：退款、纠纷或退货处理中可能损失的金额。

同一笔商品成本在同一时点只能归入一个库存/履约资金阶段，防止资金占用汇总重复计算。

### 10.5 默认费用和修改规则

- 运费、包装费和渠道费用支持全局默认值。
- 渠道可覆盖全局默认值，订单可覆盖渠道默认值。
- 系统必须显示费用来源：全局默认、渠道默认、导入、API、采购单或人工修改。
- 实际费用未产生前使用预计值；产生后由实际值替换，并保留差异。
- 已完成订单修改金额、成本或费用时，必须填写原因并生成审计记录。

## 11. 发货取证规则

- 快递发货订单默认提示上传一段视频；自提、无需物流或供应商直发可按渠道规则关闭提示。
- 视频提醒包含商品及配件、状态或功能、封箱及面单三个部分，但不强制逐项打勾。
- 未留证只产生提醒，不阻止发货。
- 视频记录必须关联内部订单 ID，不依赖可能变化的外部订单号或客户昵称。
- 上传完成的判定必须同时满足文件已持久化和数据库元数据已保存。
- 视频保存在私有对象存储，查看和下载使用短时授权链接，不生成永久公开地址。
- 上传失败或网络中断不得改变订单和库存状态，用户可以安全重试。
- 删除或归档视频属于审计操作；具体自动保留期限在 M5 前确认。

## 12. 客户与风险规则

- 闲鱼客户优先使用平台稳定买家标识；昵称不作为唯一键。
- 非闲鱼客户可以使用经确认的手机号、微信号或其他渠道标识。
- 系统不根据相同姓名或昵称自动合并客户。
- 跨渠道合并必须由用户确认，并保留原标识、订单关系和合并审计。
- 风险依据只来自可追溯的订单、未付款、取消、退款、退货、纠纷、沟通备注或用户标签。
- 风险提示必须展示原因，不自动取消订单或拉黑客户。
- 客户仍有进行中订单、退款、退货或纠纷时，延后产生拉黑提醒。
- 所有关联交易和售后结束后，才可产生“建议拉黑”待办；用户自行在闲鱼执行并回填结果。

## 13. 权限规则

### 13.1 管理员

- 查看和维护全部订单、库存、采购、客户、资金、利润、视频和报表。
- 配置销售渠道、默认费用、风险规则、用户、密钥和外部接口。
- 确认大额库存调整、坏账核销和已完成订单金额修改。
- 执行导入、导出、备份、恢复和敏感数据管理。

### 13.2 操作员

- 查看并处理被授权的订单、商品、采购、库存和发货取证。
- 创建渠道订单、记录收款和费用，但不能确认坏账核销。
- 可提交盘点结果，不能自行确认大额盘亏或系统级调整。
- 不可查看或修改系统密钥、完整备份、用户权限和系统级安全设置。
- 默认不查看全局利润报表；订单处理所必需的售价和成本可按后续权限设计细化。

### 13.3 手机上传页

- 二维码上传页使用一次性、短时有效的授权令牌。
- 令牌只能访问指定订单的上传能力，不能浏览其他订单或后台数据。
- 上传完成、过期或管理员撤销后令牌失效。

## 14. 核心异常处理

| 异常 | 系统行为 | 禁止行为 |
|---|---|---|
| API 超时或授权失效 | 保留已有数据、记录失败、提示导入或手工兜底 | 把已有字段覆盖为空、无限重试 |
| 重复付款或状态事件 | 命中幂等键并返回已有处理结果 | 重复锁库存或重复记账 |
| 状态乱序到达 | 保存原事件，按事件时间和合法转换规则评估 | 无条件用晚到请求覆盖当前正确状态 |
| 库存不足 | 锁定可用部分并记录缺口，生成采购待办 | 静默形成负库存 |
| 发货前未留证 | 明确提醒并允许继续 | 强制阻止发货 |
| 视频上传中断 | 保留订单，标记失败并允许重试 | 显示已留证或删除订单数据 |
| 收款超过应收 | 标记超额收款待处理 | 自动算作额外利润 |
| 已完成但未收款 | 进入待收款异常清单 | 计入完整已实现利润 |
| 已收款但未交付 | 进入待履约清单 | 自动标记完成 |
| 退货已到但未验收 | 进入待检库存 | 自动加入可售库存 |
| 历史导入导致负库存 | 生成核对待办 | 伪造采购或入库记录 |
| 客户身份疑似重复 | 给出候选并等待用户确认 | 自动合并客户档案 |

## 15. P0 核心验收规则

### 15.1 订单与库存

1. 闲鱼未付款订单进入系统后不改变可售库存。
2. 同一订单首次确认付款时锁定库存，重复付款事件不增加锁定量。
3. 两个订单同时锁定最后一件商品时，最多一个订单成功锁定，另一个显示缺货。
4. 订单取消后只释放未出库数量，已经出库的数量不能通过取消直接回补。
5. 发货时实际库存和锁定库存同步减少，可售库存不发生二次扣减。
6. 退货签收后先进入待检，只有验收可售后才增加实际库存。
7. 盘点调整保留调整前后数量、原因、时间和操作人。

### 15.2 资金与利润

1. 闲鱼买家付款后显示平台托管待结算，不显示已回款。
2. 渠道订单部分收款后正确显示已收和待收金额，不计入完整已实现利润。
3. 全额回款且履约完成后，按实际成本和费用计算已实现利润。
4. 退款从净收款扣减一次，不能同时作为费用再次重复扣除。
5. 供应商更新报价后，历史库存批次和已完成订单利润保持不变。
6. 修改已完成订单成本时必须填写原因，并可查看修改前后值。
7. 同一商品成本在资金占用分类中不得重复统计。

### 15.3 渠道销售

1. 渠道草稿订单不锁库存，确认后才锁定。
2. 渠道订单与闲鱼订单争用同一 SKU 时使用同一可售库存。
3. 无外部订单号的渠道订单可以通过内部订单号安全更新。
4. 已完成未收款和已收款未完成订单分别出现在对应异常清单。
5. 渠道订单取消、退货和退款正确联动库存、资金、利润和客户档案。
6. 渠道看板与全渠道汇总可以通过订单明细核对，不重复计算。

### 15.4 取证、客户与权限

1. 手机扫码只能向指定订单上传视频，不能浏览后台其他信息。
2. 上传中断后订单仍存在，重新上传成功后才显示已留证。
3. 未留证订单可以发货，但工作台保留提醒。
4. 客户存在进行中交易或售后时不产生立即拉黑待办。
5. 操作员不能查看或修改密钥、用户权限和完整备份。
6. 关键金额、库存、客户合并和视频删除操作均有审计记录。

## 16. M1 评审结论与进入 M2 的条件

> 评审结论：2026-09-06，产品负责人确认无异议，M1 通过。

本规则包通过评审需满足：

- 产品负责人确认核心状态、库存动作和利润口径符合实际经营。
- 没有会改变核心订单、库存、资金模型的未决问题。
- 延后确认的问题已经标注最晚决策阶段和默认处理方式。
- PRD、完成计划、卡点确认单与本规则包不存在相互矛盾。

通过后，M1 标记完成并立即进入 M2，输出技术实现总文档、数据库设计、状态机、接口契约、部署与安全设计以及测试计划。
