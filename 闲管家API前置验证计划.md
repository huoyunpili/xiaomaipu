# 闲管家 API 前置验证计划

> 版本：V0.1  
> 日期：2026-09-06  
> 状态：立即执行；M2A 质量门禁  
> 目的：在大规模开发前确认闲管家 API 对当前个人鱼小铺到底可用到什么程度

## 1. 为什么现在验证

系统的核心订单、库存、资金和渠道销售本来就可以通过手工录入和批量导入运行，因此不会因为 API 不可用而全部作废。但是，如果把自动同步、售后、结算或推送能力当作既定事实，直到后期才发现套餐或权限不支持，仍会造成适配器返工和自动化预期落空。

正确做法是把 API 分成两个阶段：

- **M2A 前置验证**：现在确认准入、费用、权限、字段、限制和最小真实调用。
- **M6 正式集成**：只开发 M2A 已经证明可用的能力。

## 2. 当前公开资料能够确认什么

截至 2026-09-06，公开的闲管家开放平台资料显示：

- 存在开放平台生产网关和基于 AppKey/AppSecret、时间戳、请求正文的签名机制。
- 公开目录包含店铺授权、商品、订单列表、订单详情、发货、售后、推送和资金等类别。
- 订单同步建议先接收推送，再调用订单查询取得详情。
- 订单列表说明 `update_time` 仅支持最近 6 个月，并标注列表累计最多获取 1 万条。
- 店铺授权结果包含是否为鱼小铺、已开通服务项以及保证金是否足够。

这些信息说明“存在可对接的技术接口”，但不能证明当前个人账号自动拥有全部接口，也不能证明售后、资金、推送和历史数据完全满足本项目。

## 3. 必须取得的商务与权限结论

产品负责人需要通过闲管家官方渠道获得书面答复，问题一次性发送，减少往返：

1. 已开通鱼小铺的个人卖家，能否创建或申请“我有自研系统”应用？
2. 需要订购哪个版本或服务项？月费、年费、接口费和保证金分别是多少？
3. 订单列表、订单详情、售后退款、资金结算和订单推送是否均向该账号开放？
4. 是否提供测试环境、测试店铺或试用期？
5. AppKey/AppSecret、店铺授权码和回调地址分别如何申请和配置？
6. 每个接口的日/月调用量、QPS、并发和超限策略是什么？
7. 订单列表“最近 6 个月、累计最多 1 万条”的限制具体按店铺、查询还是账号计算？
8. 新订单、付款、发货、签收、交易成功、退款和纠纷分别有哪些推送？失败是否重试？
9. 资金接口能否确认卖家实际结算到账，而不只是买家已付款或交易成功？
10. 授权有效期、续期方式、套餐到期后的数据访问和历史订单范围是什么？

答复截图或邮件可以保存到项目私有资料，但公开仓库只记录脱敏结论。

## 4. 双方分工

### 4.1 产品负责人

- 登录闲管家后台，确认当前套餐、服务项和是否可以创建自研应用。
- 向官方发送第 3 节的问题并保存答复。
- 申请测试或正式 AppKey/AppSecret 和店铺授权。
- 准备一笔普通订单、一笔完成订单和一笔售后订单用于只读验证。
- 不在聊天、文档、截图或 Git 仓库中发送完整 AppSecret。

### 4.2 Codex

- 整理公开接口清单和字段矩阵。
- 创建不写业务数据库的只读 API 探针。
- 实现签名、超时、脱敏日志和请求结果摘要。
- 在用户将密钥安全写入本地 `.env` 后运行探针。
- 生成脱敏契约样例、能力结论和 M6 范围调整。
- 对任何有副作用的接口先说明影响，未经明确确认不调用发货、改价、下架或删除接口。

## 5. 凭据安全流程

- Codex 创建 `.env.example`，仅包含变量名和说明。
- 真实 `.env` 必须加入 `.gitignore`，文件权限限制为部署用户可读。
- 产品负责人自行在本地或服务器填入真实值，不把值写进对话和 Markdown。
- 探针日志只记录 AppKey 末四位、接口名、状态码、耗时、request_id 和脱敏结果。
- 禁止输出 AppSecret、签名原文、完整手机号、收货地址和预签名访问地址。
- 验证结束后如凭据曾在不安全位置出现，应立即重置。

## 6. 最小探针范围

### P0 只读探针

| 顺序 | 探针 | 目的 | 通过条件 |
|---:|---|---|---|
| 1 | 签名自检 | 确认正文序列化、时间戳和签名算法 | 本地固定向量测试通过 |
| 2 | 店铺查询 | 确认凭据、卖家 ID、授权期限和服务项 | 返回当前鱼小铺且信息可解析 |
| 3 | 最近 1 天订单列表 | 确认分页、时间和金额单位 | 与后台抽样订单数量基本一致 |
| 4 | 指定订单详情 | 确认订单、商品、买家、物流和时间字段 | 关键字段覆盖矩阵可填写 |
| 5 | 已完成订单详情 | 检查交易成功与结算字段 | 能判断哪些字段可用于回款状态 |
| 6 | 售后/退款查询 | 检查退款金额、状态和关联订单 | 至少一笔样例可稳定关联 |
| 7 | 资金/结算查询 | 判断是否能确认实际到账 | 有明确结算字段，或确认必须人工补充 |
| 8 | 推送验签测试 | 验证事件类型、重复投递和失败重试 | 回调可验签、落库和幂等处理 |

### 延后验证的有副作用接口

- 订单物流发货。
- 无需物流发货。
- 修改价格、地址或运单号。
- 商品创建、编辑、上下架和库存编辑。

只有在专门测试商品/订单、可撤回、无真实客户影响且用户明确同意时才调用。

## 7. 字段覆盖矩阵

每项用“完整、部分、缺失、需人工确认”记录：

- 店铺稳定标识和授权有效期。
- 订单号、状态、下单、付款、发货、签收、确认和关闭时间。
- 买家稳定标识、昵称及隐私字段可见性。
- 商品 ID、SKU ID、外部编码、规格、数量和成交价。
- 实付、退款、运费、平台费用和结算金额。
- 物流公司、运单号和签收状态。
- 售后申请、退款、退货、纠纷状态和时间。
- 推送事件 ID、事件时间、重试和乱序行为。
- 分页、时间窗、历史范围和调用配额。

矩阵直接决定内部字段所有权：API 缺失的字段必须允许导入或手工维护，API 空值不得覆盖已有内部数据。

## 8. A/B/C 决策标准

### A：关键能力可用

同时满足：

- 准入和费用可接受。
- 订单列表、详情、关键状态和售后可读。
- 推送或增量查询足以覆盖日均 20～50 单。
- 身份、金额、时间和状态字段可稳定映射。

处理：M6 实现自动同步，导入和手工保留为故障兜底。

### B：部分可用

典型情况：

- 有订单但缺结算或签收状态。
- 推送不完整，只能定时查询。
- 售后或资金接口不开放。
- 历史范围有限，但新订单可稳定同步。

处理：只自动同步已验证字段，缺失部分进入待人工确认或批量导入，不伪装为全自动。

### C：不可用或无法验证

任一情况成立：

- 个人账号无法准入。
- 套餐、保证金或费用不可接受。
- 无法获得凭据或正式答复。
- 配额不足、关键接口缺失或数据质量不能支撑。

处理：停止完整 API 开发；CSV/XLSX 增量导入和手工录入成为 MVP 正式主路径，保留适配器边界供未来重试。

## 9. 时间与停止条件

- 文档核查与探针骨架：0.5～1 个 AI 协作有效工作日。
- 官方答复和账号审批：目标 3～10 个自然日，单独记录，不计为开发工时。
- M3 工程初始化可与等待并行，但只开发通用适配器和模拟契约。
- M6 开始前必须形成 A/B/C 结论。
- 到 M6 门禁仍无权限或书面结论时，自动按 C 处理，不继续等待或基于猜测编码。

## 10. 验证交付物

- 《闲管家 API 能力与费用确认记录》
- 《闲管家 API 字段覆盖矩阵》
- `scripts/xgj_probe/` 只读探针及测试
- 脱敏契约样例
- 状态映射表和字段所有权表
- A/B/C 可行性结论
- 更新后的 M6 开发范围、工期和风险清单

## 11. 当前立即行动

1. 产品负责人在闲管家后台检查是否能进入“我的应用/添加应用/我有自研系统”。
2. 将第 3 节十个问题发送给闲管家官方支持并保存书面答复。
3. Codex 在 M3 工程初始化时创建安全的凭据模板和只读探针骨架。
4. 获得凭据后先验证店铺查询、订单列表和订单详情，再继续售后、资金和推送。

任何时候都不需要把 AppSecret 发给 Codex 对话；只需安全写入本地环境文件后告知“已配置”，即可运行验证。

## 12. 公开参考

- 闲管家开放平台接入说明：https://s.apifox.cn/35842026-8755-4ac7-a61e-9b07fbcac599/doc-2686716
- 查询订单列表：https://s.apifox.cn/35842026-8755-4ac7-a61e-9b07fbcac599/api-95074856
- 查询订单详情：https://s.apifox.cn/35842026-8755-4ac7-a61e-9b07fbcac599/api-93586385
- 授权闲鱼店铺：https://apifox.com/apidoc/shared/35842026-8755-4ac7-a61e-9b07fbcac599/api-133688165
