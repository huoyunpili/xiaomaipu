# 闲鱼小卖家后台管理系统：开发流程与质量保障计划

> 版本：V0.1  
> 日期：2026-09-05  
> 状态：执行基线  
> 关联文档：《PRD-闲鱼小卖家后台管理系统-MVP.md》

## 1. 目标

本计划用于保证系统在持续开发过程中具备以下特性：

- 订单、库存、资金和利润计算正确。
- API 故障、授权过期或网络异常时，已有业务仍可继续处理。
- 批量导入可以安全重复执行，不制造重复订单或重复流水。
- 视频上传失败可以恢复，已保存证据不会因普通操作意外丢失。
- 每次版本升级可回滚应用，并可恢复数据库和文件。
- 问题发生后能够通过日志、事件和操作记录快速定位。
- 功能迭代不会频繁破坏已经可用的核心流程。

“稳健”不等于绝对没有 Bug，而是通过设计、评审、自动化测试、监控和恢复机制降低缺陷数量，并控制缺陷造成的影响。

## 2. 开发阶段与质量门禁

任何阶段只有完成对应产物并通过退出条件后，才进入下一阶段。

### 阶段 0：项目治理与工程初始化

产物：

- Git 仓库和忽略规则。
- README、开发约定和目录结构。
- 本地开发环境说明。
- 环境变量示例文件，不包含真实密钥。
- 基础格式化、静态检查、测试和构建命令。
- 持续集成流水线。

退出条件：

- 新环境可以按照 README 启动项目。
- 一条命令可以执行格式检查、静态检查和测试。
- 提交中不存在真实账号、Cookie、AppSecret 或客户数据。

### 阶段 1：需求基线评审

产物：

- 经过确认的 PRD。
- 核心业务流程图。
- 状态字典和名词表。
- P0/P1/P2 范围。
- 待确认问题的处理结论。
- 每个 P0 功能的验收标准。

重点评审：

- 自有库存、付款后采购和供应商直发的真实比例及流程。
- 订单主状态、售后状态和资金状态是否能覆盖实际情况。
- 预估利润与已实现利润的口径。
- 视频取证的操作成本与保留期限。
- API、批量导入和手工录入之间的冲突处理规则。

退出条件：

- P0 范围无重大歧义。
- 核心状态和金额口径得到用户确认。
- 未确认事项均有负责人、验证方式或默认处理方案。

### 阶段 2：架构与详细设计

产物：

- 技术架构说明。
- 数据库实体关系设计。
- API 接口契约。
- 闲管家适配器设计。
- 闲管家 API 前置可行性报告：准入、费用、配额、字段覆盖、最小真实调用和 A/B/C 结论。
- 批量导入字段映射和冲突策略。
- 文件/对象存储设计。
- 权限、隐私和安全设计。
- 错误码、日志和审计设计。
- 备份、恢复和部署方案。

必须明确的技术决策：

- 金额存储和舍入规则。
- 时间、时区和日期边界规则。
- 订单及外部事件幂等键。
- 库存锁定、释放和出库的事务边界。
- 利润重算与历史成本快照规则。
- API 数据与内部手工数据的字段所有权。
- 视频原文件、预览文件和数据库记录的一致性。

退出条件：

- 核心表结构可以表达 PRD 中的全部 P0 场景。
- 关键接口拥有请求、响应、错误和幂等约定。
- 已完成高风险流程的设计评审。
- 不存在必须依赖未确认外部接口才能启动的核心功能。
- 已启动闲管家 M2A 前置验证；未经真实验证的能力不得写入完成承诺，外部超时按导入主路径处理。

### 阶段 3：技术骨架与纵向切片

先实现一条最小但完整的业务链：

`新增/导入订单 → 查看待发货 → 上传取证视频 → 标记发货 → 更新回款 → 计算利润`

该阶段不追求功能数量，重点验证：

- 前后端、数据库和文件存储能稳定协作。
- 状态变更和资金计算模型可行。
- 手机上传视频流程可用。
- 自动化测试和部署流程真实有效。

退出条件：

- 纵向切片具备单元、集成和端到端测试。
- 重复提交不会产生重复订单或重复流水。
- 模拟服务异常后数据保持一致。
- 测试环境可重复部署。

### 阶段 4：按模块迭代 P0 功能

建议顺序：

1. 订单、状态和操作日志。
2. 商品、SKU、供应商和报价。
3. 采购与库存流水。
4. 订单成本、资金和利润。
5. 取证视频完整流程。
6. 客户档案和标签。
7. 非闲鱼渠道销售和分次收款。
8. 工作台待办和预警。
9. Excel/CSV 增量导入。
10. 闲管家 API 同步和故障降级。

每个模块都必须经过：

`需求确认 → 设计 → 实现 → 自动化测试 → 自测 → 验收 → 合并`

退出条件：

- 全部 P0 验收标准通过。
- 不存在未处理的严重或高优先级缺陷。
- 核心自动化回归测试稳定通过。

### 阶段 5：系统测试与发布候选版

必须执行：

- 核心业务端到端测试。
- API 正常、超时、重复推送、乱序和授权过期测试。
- CSV/XLSX 正常、重复、缺字段、错误格式和大文件测试。
- 库存并发锁定与取消释放测试。
- 退款、部分退款和成本调整后的利润测试。
- 视频上传中断、重试、下载权限和删除审计测试。
- 数据库迁移测试。
- 备份与恢复演练。
- 权限和敏感数据检查。
- 基础性能和容量测试。

退出条件：

- 发布检查表全部通过。
- 恢复演练成功且步骤有记录。
- 已知问题有影响说明和规避方式。
- 形成可部署的版本号和变更说明。

### 阶段 6：灰度上线与稳定期

上线策略：

1. 先使用测试数据验证生产环境配置。
2. 导入少量真实历史订单，只读核对。
3. 选择少量新订单完整走通。
4. 连续观察同步、库存、利润和视频上传。
5. 确认稳定后再扩大到全部订单。

稳定期重点：

- 每日核对订单数量和金额。
- 检查同步失败、导入失败和异常状态。
- 检查视频上传成功率和存储容量。
- 检查备份结果。
- 收集实际操作耗时和多余步骤。

## 3. 测试策略

### 3.1 单元测试

适用范围：

- 状态映射。
- 风险规则。
- 拉黑建议的触发、延后、忽略和完成规则。
- 利润计算。
- 金额舍入。
- 库存可售量计算。
- 商品销量趋势、库存可售天数和预计降价损失计算。
- 缺少销量、市场价过期、零销量和负毛利等商品风险边界情况。
- 市场情报事件日期解析、未来 30 天窗口和跨月/跨年边界。
- 新闻来源可信度、型号匹配、重复转载合并和提醒升级规则。
- 导入字段转换和校验。
- API 签名和响应解析。

核心业务规则必须使用表格化测试覆盖正常值、边界值和异常值。

### 3.2 集成测试

适用范围：

- 数据库事务。
- 订单、库存和资金联动。
- 闲鱼与非闲鱼渠道订单共用库存、资金和利润底账。
- 订单成交、供应商报价和库存变化后商品风险评估的联动。
- 每日情报采集、文章去重、事件聚合、商品匹配和风险待办联动。
- 重复事件幂等处理。
- 文件上传与元数据保存。
- 导入任务和错误行处理。
- 外部接口适配器。

集成测试使用独立测试数据库和测试存储，不接触生产数据。

### 3.3 契约测试

- 为闲管家接口保存脱敏后的响应样例。
- 验证外部字段缺失、类型变化和新增字段时系统的行为。
- 业务代码只依赖内部统一模型。
- 外部接口变化时优先修改适配器，不扩散到业务模块。
- 搜索、新闻或内容数据源通过独立适配器接入，并使用脱敏固定样例验证字段变化。

### 3.4 端到端测试

优先覆盖：

- 登录。
- 新增和导入订单。
- 非闲鱼渠道快捷开单、部分收款和全渠道汇总。
- 订单状态流转。
- 采购及库存变化。
- 视频扫码上传。
- 退款后的利润变化。
- 高风险客户在交易或售后进行中不产生立即拉黑待办，流程结束后再提醒。
- 商品销量下降或市场参考价变化后产生可解释的库存风险提醒，但不自动改价。
- 未来 30 天新品或促销事件具备原始来源、正确匹配和库存敞口时产生醒目提醒。
- 同一新闻转载不重复提醒；事件延期、撤回或被用户标记无关后正确更新状态。
- API 故障后使用批量导入继续工作。

端到端测试数量不宜过多，重点覆盖最有价值且最容易造成损失的流程。

### 3.5 非功能测试

- 权限越权测试。
- 敏感数据泄露检查。
- 大文件和弱网上传测试。
- 一万行导入性能测试。
- 十万订单查询性能测试。
- 备份恢复测试。
- 磁盘/对象存储不可用测试。

## 4. 缺陷等级

### S0：阻断

- 数据库或证据文件大面积丢失。
- 金额或库存批量错误。
- 密钥或客户敏感数据泄露。
- 系统无法启动且无可用恢复方式。

处理：立即停止发布或暂停相关功能，优先恢复数据和业务。

### S1：严重

- 重复创建订单、重复扣库存或重复记账。
- 订单状态严重错误导致漏发、错发或错误回款。
- 视频显示已上传但原文件不可获取。
- 普通用户可以访问未授权数据。

处理：当前版本不得发布；线上出现时优先修复并回归。

### S2：一般

- 个别筛选、展示、导出或非核心操作异常。
- 有明确替代操作，不影响数据正确性。

处理：评估后纳入当前或下一个迭代。

### S3：轻微

- 文案、样式和低频体验问题。

处理：进入常规优化列表。

## 5. 完成定义（Definition of Done）

一项功能只有同时满足以下条件才能标记完成：

- 需求和验收标准明确。
- 设计与现有架构一致。
- 代码通过评审或结构化自检。
- 格式化、静态检查和自动化测试通过。
- 核心异常和边界情况已覆盖。
- 数据库变化包含迁移与回退说明。
- 日志不泄露敏感信息。
- 用户可理解错误信息并知道如何恢复。
- 必要文档、配置示例和变更记录已更新。
- 在接近生产的测试环境中完成验收。

## 6. 代码与版本管理

- 主分支保持可构建、可测试状态。
- 每项功能使用独立、小范围变更，避免超大提交。
- 提交信息说明业务目的，不只写“修改”或“修复”。
- 合并前必须通过自动化质量检查。
- 每个可发布版本使用明确版本号和变更说明。
- 不使用破坏历史的方式处理已经共享的提交。
- 不提交真实密钥、生产数据库、客户资料和证据视频。

版本建议：

- `0.1.x`：内部原型和核心模型验证。
- `0.2.x`：P0 功能开发与测试。
- `0.9.x`：真实订单灰度和发布候选版。
- `1.0.0`：第一版正式稳定发布。

## 7. 数据库与迁移纪律

- 所有结构变化通过版本化迁移脚本执行。
- 迁移脚本必须在空数据库和已有数据数据库上测试。
- 破坏性迁移采用“先新增、迁移数据、切换读取、最后清理”的多阶段方式。
- 不直接删除仍可能被旧版本使用的字段。
- 发布前备份数据库，发布后验证关键记录数量和金额汇总。
- 金额禁止使用二进制浮点数。
- 时间统一保存明确时区或 UTC，界面按 Asia/Shanghai 展示。

## 8. 外部 API 稳健性

- 设置连接和响应超时。
- 只对安全、可重试的请求自动重试。
- 订单推送以平台事件 ID 或店铺+订单号+状态构造幂等键。
- 允许事件重复、延迟和乱序到达。
- 保存原始响应的脱敏摘要，便于排查字段变化。
- 同步任务有成功数、失败数、最后游标和最后成功时间。
- API 失败时不覆盖已有正确数据为空值。
- 授权过期后停止无意义重试并提醒重新授权。
- 始终保留批量导入和手工维护入口。

## 9. 可观测性

系统至少监控：

- 服务是否正常运行。
- API 同步成功率和最后成功时间。
- 每日市场情报任务的最后成功时间、来源成功率、候选文章数、去重数和高风险事件数。
- 推送接收量、重复量和失败量。
- 导入任务成功率和错误行数。
- 视频上传成功率、失败率和存储用量。
- 数据库备份时间和结果。
- 待人工确认订单数量。
- 异常库存流水和负库存。

日志应包含请求或任务追踪 ID，但不得包含完整 AppSecret、Token、手机号、地址和视频公开链接。

## 10. 发布与回滚

每次发布前：

- 固定版本号。
- 运行全部质量检查。
- 备份数据库和关键配置。
- 验证迁移脚本。
- 生成变更说明和已知问题。
- 明确应用回滚和数据库恢复方式。

每次发布后：

- 验证登录、订单查询、导入、利润和视频上传。
- 对比发布前后的订单数、资金汇总和库存汇总。
- 检查错误日志和资源使用。
- 小范围运行稳定后再全面启用新能力。

如果新版本影响订单、库存、资金或证据文件正确性，应立即停止相关入口并执行回滚或修复方案。

## 11. 当前下一步

M1 业务规则已于 2026-09-06 通过。2026-09-07 已完成 M2 结构化评审和 M3 本地质量门禁：12 项测试通过，Linux 镜像构建成功；详见《docs/M2-技术评审与M3执行记录.md》。以下第 1～3 项已完成基础实现，下一步从第 4 项推进，Outbox 消费随业务事件实现：

1. 评审《技术实现文档-闲鱼小卖家后台管理系统-MVP.md》并完成 M2 技术门禁。
2. 初始化工程、依赖锁、数据库迁移、质量检查和持续集成。
3. 建立登录、权限、审计、幂等和事务消息基础。
4. 实现“商品库存 → 手工订单 → 锁定 → 出库 → 回款 → 利润”的最小纵向切片。
5. 为并发锁库存、重复事件、退款利润和事务回滚编写自动化测试。
6. 通过纵向切片评审后，再按完成计划推进 P0 模块。
