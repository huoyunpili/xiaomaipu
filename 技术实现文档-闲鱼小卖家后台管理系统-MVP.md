# 技术实现手册：PRD V1.31

本次实现以 PRD V1.31 为唯一产品范围。旧 T0–T5 记录保留为历史，不代表新版本验收通过。

> 本文描述目标实现及开发约束，不等于所有功能已经完成。实际状态以项目完成计划和对应验证记录为准。`docs/V1.31-实施基线.md` 是本轮启动时的基线快照。

## 1. 代码入口与调用链

| 层次 | 当前入口 | 责任与约束 |
|---|---|---|
| 平台接入 | `app/integrations/services.py` | 查询、规范化、时间版本检查、持久化平台快照；仅接受通过乱序检查的数据进入业务投影 |
| 业务投影 | `app/workbench/services.py::project` | 按店铺和订单号合并 Trade，保留本地成本和人工处理字段；不得绕过接入日边界 |
| 领域模型 | `app/workbench/models.py` | 成本、逐单金额、担保及参考日计算，整单状态，不拆分履约 |
| 页面与操作 | `app/workbench/views.py`、`urls.py` | 查询筛选、权限、事务、导出前刷新、审计；页面和导出共用统计集合 |
| 历史导入 | `app/workbench/importing.py` | UTF-8 CSV，逐行验证，整批原子写入，订单号幂等 |
| 长图 | `app/static/workbench-export.js` | 从不可变批次文本生成 PNG，自动换行和分张，不向远端传输地址 |
| 展示 | `app/templates/workbench/`、`app/static/workbench.css` | 七个主入口，桌面及手机布局，空状态和失败状态区分 |
| 数据回填 | `app/workbench/management/commands/rebuild_workspace.py` | 使用已保存平台快照重建投影；旧快照缺少的敏感字段不能凭空补全 |

后台自动同步和手动刷新必须走同一接入与投影链。平台快照负责来源追溯，Trade 负责经营展示；禁止在模板中另写一套利润或退款判断。

## 2. 数据契约与状态处理

### 2.1 字段归属

平台字段包括订单号、订单及退款状态、商品标识/规格/数量、实付金额、各业务时间、收件信息和物流。成本、默认供应商、单次供应商覆盖、追回确认和运费损失属于本地维护字段。同步只能更新平台归属字段，不能重置成本快照、追回确认或人工备注。

时间以带时区时间存储，展示和自然日筛选使用 Asia/Shanghai；API Unix 时间按秒解析，空值、非法类型和越界时间不能代替为当前时间。金额使用整数分，入库前拒绝负数、非整数和越界值。字符串不得因静默截断而把不完整地址当作可以发货的完整地址。

商品成本匹配键应稳定对应商品标识和规格。CSV 与 API 使用不同规格字段时必须统一匹配语义，避免同款分裂为两份成本配置。旧订单已经匹配到的成本和版本保存不变；商品改型不能沿用错误的旧规格成本。

当前匹配实现由 `resolve_product` 统一执行：优先完整标识；API 订单可复用相同商品/规格的旧 CSV 成本；CSV 未填写 SKU 时，通过本店已有平台订单反查唯一成本记录。候选不唯一时拒绝自动选取，要求填写可选“规格标识”列。模板“商品标识”指管家 product_id，旧 16 列 CSV 仍兼容；历史订单按店铺/订单号与后续 API 快照关联，不因为接入日前的日期再次生成订单。

### 2.2 主状态及不变量

| 条件 | 主状态/处理 | 担保 | 利润 |
|---|---|---|---|
| 明确未付款 | 未付款，不进入履约队列 | 0 | 不参与 |
| 已付款且有效、平台明确未发货 | 待发货 | 实付 | 已知成本才计算预计 |
| 平台明确整单已发货 | 待完成 | 实付 | 已知成本才计算预计 |
| 退款申请或客服介入 | 退款处理中，暂停发货及参考日 | 未实际退款时保留实付，单列风险 | 不混入正常预计/实际 |
| 平台验证的全额退款完成 | 已退款，保留原发货历史，创建追回待办 | 0 | 销售及平台费归零；运费损失单列 |
| 平台交易成功且有完成时间 | 已完成 | 0 | 按 confirm_time 计实际 |
| 明确取消/关闭 | 已关闭 | 0 | 无有效成交时不参与 |
| 未知枚举、字段冲突、部分退款或缺失关键时间 | 待核对，记录具体原因 | 明确提示不确定金额，不静默表示全部可信 | 不虚构收益 |

退款完成必须有真实枚举及金额/时间证据，不能仅把任意非零退款状态解释成退款成功。已完成与退款状态冲突时进入明确的退款处理规则；保留原发货时间和完成时间。售后 timeout 字段未验证前不启用正常回款倒计时。

已核对官方 OpenAPI 和本机真实快照：订单 23 为已退款，24 为已关闭；退款 5 为成功，4 为退款关闭，1/2/3/8 为处理中，6 提示核对后续责任。成功退款需要有效退款时间和全额金额一致；未知代码、缺少退款状态、部分退款和矛盾时间进入待核对，首页单列这些订单的数量和已付金额。详细来源及尚未取得的实单转移见 [API 字段核验](docs/V1.31-API字段核验.md)。

### 2.3 事务与一致性

- 以数据库唯一约束保证 `(shop, number)` 不重复，重试返回同一订单。
- 商品成本更改锁定商品记录，补算缺成本订单；订单快照更正要求原因并写审计。
- 导出先刷新所选 API 订单，再重新读取状态；刷新失败不创建新发货批次。批次写入前复查资格，防止并发同步使过期对象进入批次。
- 快照签名覆盖状态、商品、规格、数量、地址、电话、供应商、备注及退款清单相关字段。变化时标明具体变动订单，旧快照不覆盖。
- API 失败保留已有记录、最后成功时间及失败提示；启动补查使用已有游标和平台允许窗口，不能重新设置接入日。

## 3. 页面与接口契约

所有页面要求登录，成本修改、订单更正、导入及新清单生成要求店主管理权限；POST 使用 CSRF。批次查询还必须检查当前店铺，敏感响应使用 no-store。

| 路由名称 | 方法 | 输入 | 输出及错误行为 |
|---|---|---|---|
| `dashboard` | GET | 无 | 发货、退款、担保、利润与回款提醒；金额下钻集合和首页一致 |
| `wb-orders/shipping/pending/refunds` | GET | 状态、供应商、搜索、日期、分页 | 互斥主状态分类；有效筛选在翻页后保持；待办按等待时间排序 |
| `wb-detail` | GET/POST | 订单 ID、本地补充信息、更正原因 | 字段错误在原页提示；成功后重定向，写审计 |
| `wb-costs` | GET/POST | 商品 ID、单件成本、默认供应商 | 建立新成本版本，自动补齐缺成本订单 |
| `wb-profits` | GET | mode、range、start、end、排序、export | 同一筛选生成汇总、趋势、表格和 CSV；非法区间返回 400 |
| `wb-export` | POST | shipping/refund、所选订单 ID | 刷新、剔除失效订单、按供应商建批次；空集合或刷新失败返回可读提示 |
| `wb-batch` | GET | 批次 ID | 历史快照、过期提示、PNG 下载；跨店及不存在批次返回 404 |
| `wb-history` | GET/POST | 模板请求或 CSV 文件 | 成功/跳过数量；错误显示行号且整批不写入 |
| `wb-settings` | GET/POST | 图片保留天数（可空） | API、商品成本、店铺、备份及历史入口；店主可保存清理期限并记录审计 |
| `wb-batch-image` | POST | 批次 ID、页码、PNG、CSRF | 校验店主和店铺后保存本机私有文件；重复页保留已保存版本 |
| `wb-image` | GET | 图片 ID | 店主鉴权后下载 PNG；不存在/已清理返回 404，no-store 与 nosniff |

利润页预计模式按付款日，实际模式按完成日，区间含首尾两天。刷新及只带 export 参数的下载沿用已保存筛选；排序不能改变统计集合。缺成本销售额单列且与汇总口径说明一致。退款风险、运费损失单列，不以 0 成本代替未知值。

## 4. 长图、隐私与本地发行

发货 PNG 包含型号规格、数量、姓名、完整电话地址、备注、供应商、生成时间、订单总数、总件数和回复单号提示。退款 PNG 包含客户姓名和电话后四位、退款状态及金额、成本快照/待退额、退货需求和单号、申请时间、等待时长、备注。长地址和规格必须换行；超长清单分为连续编号图片。

历史快照与生成文件分开处理。浏览器生成 PNG 后，通过带 CSRF 的本地接口写入 `PRIVATE_MEDIA_ROOT/exports/`，由服务端生成文件名；元数据保存批次、页码及 SHA-256。服务端验证 PNG 签名、尺寸、CRC、图片块和有界解压结果，拒绝损坏、超大及路径越界文件。下载只允许本店管理员；图片不进入静态目录、普通日志或 Git。

`WorkspaceSettings.image_retention_days` 留空表示永久保留，设置正整数后每日 Celery 任务按生成时间清理过期 PNG 和对应图片元数据，保留批次及快照。可以运行 `manage.py cleanup_exports --dry-run` 查看数量，再按已配置期限执行 `manage.py cleanup_exports`。历史批次重新打开可重新生成已清理图片。自动清理依赖本地 Worker/Beat 正常运行，关机休眠期间暂停。

发行复用 `scripts/build_release.ps1`、`scripts/local_release.ps1` 及 deployment 配置。新用户创建自己的管理员并配置自己的 API；不包含开发账号、密钥、真实订单或激活机制。升级前备份数据库和私有文件，运行迁移后验证新旧数据；失败时恢复同一备份与原程序版本。恢复演练必须覆盖 Trade、成本版本、批次、追回记录、账号和供应商映射，不能只核对表数量。

## 5. 开发与验收命令

使用仓库锁定依赖和隔离 PostgreSQL，测试不得修改真实经营库。Windows 从仓库根目录执行：

```powershell
.venv\Scripts\python.exe -m pytest tests/test_workspace.py -q
.venv\Scripts\python.exe scripts/check.py
```

`scripts/check.py` 顺序执行 Ruff、格式检查、mypy、Django check、迁移漂移检查和全量 pytest。浏览器测试需要 Playwright 浏览器及隔离数据库可用。迁移检查只证明模型和迁移一致，不证明升级恢复成功。发布演练和桌面/手机视觉检查另行保存记录。

完整验收映射与待完成项见项目完成计划。实际平台枚举验证、安装恢复验证和浏览器截图缺少证据时一律保持未完成。

## 技术方案

沿用 Django、PostgreSQL、Celery 和现有本地发行脚本。新增 workbench 业务模块，独立于旧采购库存模型；平台订单入库后自动投影，不再要求用户逐单转成销售草稿。旧账及库存数据不删除。

| 实体 | 职责 |
|---|---|
| Trade | 平台/导入订单、状态、收件信息、时间、成本快照、本地售后备注 |
| ProductCost | 商品/规格匹配键、成本版本、默认供应商 |
| CostVersion | 每个商品成本版本的单件金额和生效时间；供晚同步订单按付款时间匹配 |
| ExportBatch | 不可变的发货或退款清单快照、供应商、生成时间、状态变化提示 |
| ExportImage | 私有 PNG 的批次页码、路径及内容摘要，重复生成不覆盖已保存页 |
| WorkspaceSettings | 单店实例的图片保留期限；默认永久保留 |
| AuditEvent | 成本更正、运费修改、退款追回确认的操作记录 |

金额统一使用整数分；1.6% 平台费逐单四舍五入到分，汇总逐单结果。北京时间自然日筛选；预计按 pay_time，实际按 confirm_time。全额退款利润为零，选填运费损失单列。缺成本不按零成本计算。

成本版本实现：`0002_cost_versions` 建立版本表并保存已有商品的当前版本。新增价格保留版本和生效时间；未付款订单不保存成本快照；已付款但晚同步的订单选择付款时间之前最近的版本。若付款早于首次配置，使用首次配置版本补齐原本缺成本的订单。已有快照始终优先，后续调价不重写。升级只能还原旧库已有的当前版本时间，不能凭空恢复此前没有记录的价格历史。

利润展示与导出：六项汇总使用已知成本订单，缺成本订单的实付额单列；表格运费损失列不混入成交利润。日期、商品和利润高低排序仅改变顺序，不改变统计集合。CSV 使用北京时间，包含逐单记录、已知成本合计及区间内单列的售后损失，并标明缺成本记录不计入利润合计。刷新保存模式、范围和排序，下载沿用相同参数。

批次生成在 API 刷新后重新锁定并读取订单，拒绝跨店混合导出；数据库已变更为退款或已发货的订单不会因调用方持有旧对象而进入发货单。历史页逐单比较快照签名并列出已变化订单，保留旧清单原文。

担保金额仅包括已付款、未完成、未关闭、未全额退款订单；退款申请计风险而非完成退款。正常发货参考回款日默认 consign_time + 10×24 小时；退款中暂停参考。未知平台状态保持待核对，不能自动变成交易成功。

统一参考规则由 `WorkspaceSettings.reference_days` 保存（迁移 `0005_reference_days`，默认 10）。设置页店主可填写 1–365 天，审计记录修改前后天数；自定义天数明确标为经营参考，实际确认时间不受影响。查询预加载店铺设置，避免逐单重复查询。图片保留期限独立保存，不相互覆盖。

首页今日参考回款、未来三天和超过参考时点链接至待完成明细；退款风险链接至仍处担保中的退款订单。`repayment.py` 统一日期窗口：北京时间当日零点至次日零点、当日零点至第三日结束、当前时刻之前，均为左闭右开。今日和已经过时点可重叠；列表查询、再次筛选、分页和 CSV 保留同一 `due` 参数。退款中、已完成以及无付款事实订单不进入参考回款统计。

历史订单使用 UTF-8 CSV 模板导入，同一店铺订单号唯一；后续同步更新平台字段，不覆盖用户成本快照和售后备注。补填商品成本一次会补齐同类型缺成本订单；更改当前成本仅影响新订单，旧快照更正必须填写原因。

商品配置包含默认供应商、微信备注名称和默认发货备注（`0006_product_notes`）。API 新订单与历史 CSV 复用配置；修改默认发货备注会更新当前待发货订单，已发货订单保留原有备注。订单保存供应商微信备注快照，本单更换供应商后不再随商品默认值更新；仅补充其他信息不触发供应商覆盖标志。只改供应商或备注不增加成本版本。

商品配置按店铺归属隔离，唯一键为 `(shop, key)`。API/CSV 匹配、设置页读取和提交、同款订单回填均限定对应店铺。`0007_product_shop` 根据已有订单关联确定归属；跨店共用的旧配置按店铺复制，复制成本版本及生效时间，再重新关联订单，保留订单已有金额和版本快照。无订单关联的旧配置归属为空并从经营界面隐藏，不推断归属。该数据拆分迁移不提供逆向合并，回退应恢复升级前备份。

发货备注分为商品默认、平台卖家备注和本单补充，分别保存后按顺序合并，完全相同的内容去重。平台同步只写平台备注，不抹掉本单补充。旧库已有备注继续保留在本单补充字段，不猜测原来源。清单快照保存最终合并文本；修改影响内容时旧批次标记过期，原始文本和已保存 PNG 不变。微信备注用于本地查找供应商，收件人清单不附带该联系备注。

发货导出前刷新所选 API 订单，网络失败不生成新发货单；状态改变后重新检查并排除已发货/退款/关闭订单。导出按供应商生成 PNG，使用本地字体，不请求远程图片或上传地址。批次保存在私有数据库，下载必须登录。历史批次不会因为同步而删除。

前端使用 Django 模板、响应式 CSS、少量原生 JavaScript；主导航为工作台、待发货、待完成、退款售后、利润统计、全部订单、设置。图片仅用已同步的安全 HTTP(S) URL，失败时占位，金额、状态和主要操作保持文字可读。手机核心操作不依赖横向滚动。

本版本所有能力免费；无激活码、公众号绑定、付费服务或授权服务器。API 配置保存在本机，旧连接起始日保持不变。升级先备份，再迁移，再重启；不调用退款审批、发货等平台写接口。

发行隐私：Git 忽略、归档排除及 Docker 构建排除规则覆盖运行目录、私有图片、备份、常见密钥文件和日志。打包脚本继续要求干净提交并检查禁止文件名。Gunicorn 本地访问日志仅输出状态码和耗时，不记录请求地址、搜索词、来源页或客户端地址；应用 JSON 日志只输出允许的元数据。上述规则不能代替最终发行包内容及运行日志的专项检查。

售后详情与订单列表分别更新事实。无 update_time 的售后详情按本次观察时间保存，不伪造平台版本；有版本的售后通过独立记录去重并保护乱序。确认全额退款后立即重新计算担保与利润。退款清单额外保存申请时间、生成时等待天数、退款金额、是否需要退货和退货单号；售后超时字段暂不作为回款日期。发货后 API 返回空收件字段时保留此前本机已保存的收件信息。

## 实施顺序

### 最终验收补充：状态时间与旧快照

`Trade.status_changed_at` 独立记录状态变化时间，已结束订单使用它倒序排列；重复同步、修改备注和商品配置不改变此值。`0008_status_change_time` 优先使用已有完成、退款或退款申请时间回填，缺少证据时回退旧记录更新时间，不伪造平台历史。新投影使用可信平台更新时间；无版本售后使用观察时间。

本地发行的迁移流程依次执行 `migrate`、`bootstrap`、`rebuild_workspace` 和 `check`。回填命令只处理接入范围内或已有对应经营订单的快照，不补造缺失地址或手机号；这些字段需要下一次平台详情刷新。重复回填保留本单备注、已有私有收件信息及成本快照。缺少完整收件信息时禁止生成发货批次。

订单详情说明平台/CSV 时间来源、北京时间和参考天数；退款中暂停参考回款。旧发货清单发生变化时明确提醒“联系供应商停止发货”，提供重新选择入口，原快照保持可查。

完整验收映射见 [V1.31 验收矩阵](docs/V1.31-验收矩阵.md)。

1. R1：订单模型、状态投影、API 同步接入、旧快照回填。
2. R2：商品成本版本、自动快照、利润/担保计算、退款追回和可审计更正。
3. R3：七个主页面、日期筛选、商品图片、逐单公式与汇总。
4. R4：发货/退款 PNG、过期批次、CSV 导入与导出、设置和安装引导。
5. R5：迁移与业务测试、浏览器桌面/手机验证、备份升级和执行记录。

## 验证要求

升级恢复复现：运行 `.venv\Scripts\python.exe scripts/verify_workspace_restore.py`，在自动生成的隔离库中执行旧结构→工作台→当前迁移、实际备份命令和独立恢复，并比较逐表内容与私有图片哈希。已执行结果、范围和剩余限制见 [V1.31 升级恢复记录](docs/V1.31-升级恢复执行记录.md)。

开发环境 `backup_local --verify` 新增内容摘要比较，`--output-dir` 可指定备份父目录；不指定时保持 `.local/backups`。内容摘要由 `fingerprint_database` 命令复用，只有读取操作。实际备份期间暂停写入，避免源库继续变化造成比较失败。

0.6.0 发行 PowerShell 流程已使用相同摘要实现，备份前暂停写入，在临时库比较完整内容后写入清单；恢复前检查文件哈希，恢复后先比较内容再迁移。辅助程序以只读文件挂载运行，兼容旧发行镜像的备份阶段。Windows PowerShell 5.1 中使用 Docker 完整参数名，避免与公共参数冲突。实际安装、首次建号、备份、覆盖恢复及 Upgrade 的执行证据见 [发行安装记录](docs/V1.31-发行安装执行记录.md)。

- 覆盖 PRD A1–A11，特别是退款即时扣减、完成订单移出担保、历史成本不被覆盖、时间区间边界和重复导入。
- 导出必须验证访问权限、重新同步失败、状态变化、多个供应商、中文长地址和移动端布局。
- 运行 Django 检查、迁移检查、ruff、类型检查和现有适用回归测试；修改页面导致旧 UI 断言过时则更新断言，不能忽略业务回归。
- 真实平台字段的枚举如未有证据，不宣称已验证；记录来源与限制。现有生产订单不作为测试修改对象。

## API 核对依据

2026-09-15 检查公开订单详情与列表：商品 images、sku_text、quantity、收件字段、pay_time、consign_time、confirm_time 存在。旧同步 normalize 丢弃图片与地址，新实现把所需收件字段保存到私有 Trade 表，旧平台快照继续不含支付凭证。

- https://s.apifox.cn/3ac13d69-5a38-4536-ae9b-a54001854ef8/api-93586385
- https://s.apifox.cn/3ac13d69-5a38-4536-ae9b-a54001854ef8/api-95074856
- https://s.apifox.cn/35842026-8755-4ac7-a61e-9b07fbcac599/api-472549188
