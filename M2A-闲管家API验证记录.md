# M2A 闲管家 API 验证记录

> 日期：2026-09-07  
> 状态：进行中  
> 安全范围：仅测试无副作用查询接口，不记录 AppSecret 和买家隐私

## 1. 当前账号与应用条件

2026-09-11 最新进展：已根据用户授权继续接入，店铺、订单列表、单笔详情、售后列表及更新时间过滤已实测成功；51 条平台记录已进入本地待核对表，独立 Worker 与 Beat 增量同步已通过。详细结果、契约和未覆盖项见《docs/API接入与发布查验-0.4.0.md》。下文 9 月 7 日“需约定下一次时间”属于历史约定，已由本轮明确授权更新。

- 应用类型：进销存。
- 应用模式：自研系统。
- 应用状态：已上线。
- ERP 专业版试用有效期：2026-09-12。
- 已取得 AppKey/AppSecret，具备生产网关实测条件。

## 2. 已完成验证

| 项目 | 结果 | 说明 |
|---|---|---|
| 官方接口文档可访问 | 通过 | 生产网关为 `https://open.goofish.pro` |
| 官方 Python SDK 获取 | 通过 | 版本日期 2026-06-23 |
| 签名算法核对 | 通过 | 无商家 ID 与带商家 ID 的签名参数顺序已按 SDK 校正 |
| 本机到生产网关网络 | 通过 | 服务端返回 HTTP 200，可正常建立 HTTPS 请求 |
| 鉴权及授权店铺查询 | 通过 | 2026-09-07 使用用户提供的精确凭据，HTTP 200、code=0；存在有效授权店铺 |
| 订单列表 | 通过 | HTTP 200、code=0，第一页返回 10 条，观察到状态 11/12/21/22/23；状态语义仍需契约确认 |

## 3. 下一次验证

### 2026-09-11 用户授权后的复测

- 用户明确要求继续尝试 API，并提供凭据，已按要求保存至根目录 `.env.local`（已在 `.gitignore` 排除）。本文不记录密钥。
- 授权店铺查询：HTTP 200、code=0，返回 1 家店铺且有效。
- 订单列表：HTTP 200、code=0，第一页返回 10 条。
- 对列表中的一笔订单查询详情：HTTP 200、code=0，返回非空详情数据。
- 本次只读验证未写入经营数据库，未调用发货、改价等写接口，也未启用后台自动同步。
- 当前已证实店铺、订单列表及单笔详情可读取；历史覆盖、售后退款、结算字段语义、推送和正式同步仍待验证及实现。

以下为 2026-09-07 保留的下一次验证计划；其中单笔订单详情查询已在上述复测完成。

后续连接必须与产品负责人配合，不自动重试或继续实测。上述成功查询发生在该配合约定提出之前。

1. 现有凭据已验证，无需重复索取或重新鉴权；约定下一次共同测试时间。
2. 补充核对 `is_trial`、`is_pro`、有效期和服务项。
3. 查询最近订单列表。
4. 选择脱敏订单调用订单详情。
5. 手动同步一笔授权前订单，验证它是否能被列表和详情接口读取。
6. 再验证售后、退款和推送；所有写接口保持禁用。

当前为 **B（阶段性部分可用）**：仅证实授权店铺及订单列表查询成功，不承诺订单详情、历史覆盖、售后或结算字段可用，不将原始数字状态直接映射为内部业务状态。

## 4. 当前实现

- 探针：`scripts/xgj_probe/probe.py`
- 使用说明：`scripts/xgj_probe/README.md`
- 示例配置：`.env.example`
- 密钥与探针输出已加入 `.gitignore`
